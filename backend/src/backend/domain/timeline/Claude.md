# 담당 업무: 메인 페이지 백엔드 (timeline 도메인)

> 이 문서는 작업을 재개할 때 가장 먼저 읽는 문서다. 새 작업을 시작하기 전에 '다음 작업'과
> '결정된 설계 방향'을 확인하고, 작업이 끝나면 바뀐 내용을 여기에 반영한다.

> **코드 주석 방침**: 파일 최상단에는 그 파일의 역할, 각 코드 위에는 "무엇에 쓰는지 + 어디를 고치면
> 어떻게 바뀌는지"만 적는다. 버그를 막는 주의사항은 현재형 한 줄로 쓴다. **날짜·실측 경위·"예전에
> ~해서 ~했다" 같은 이력은 코드에 쓰지 않고 이 문서에만 남긴다.**
> 동작을 바꾸면 그 동작을 설명하는 주석을 **모든 파일에서** 찾아 같이 고친다
> (`grep -rn "옛 동작 키워드" src main.py`).

## 담당 범위

- 메인 페이지 백엔드 구현 (`src/backend/domain/timeline`)
- 실사용 API(한국투자증권, 네이버 뉴스 등) 데이터 확인 및 기획된 기능의 구현 가능 여부 검증
- 프로젝트 진행 중 미흡한 부분 지원

## 메인 페이지 구성 (내 작업 대상)

- **최상단 지표 바**: 코스피, 코스닥, 니케이, 달러 환율, S&P500, 나스닥 — 모든 페이지 공통 고정 영역이지만 데이터는 이 도메인에서 제공. **구현 완료**
  - ~~나스닥 선물, 유가, 금~~ 은 제외함. 한국투자증권 API로 조회는 되지만, 지금 쓰는 계좌에 CME/NYMEX/COMEX 거래소 이용 권한이 없어서 호출하면 에러가 남(계좌 권한 문제라 코드로 해결 불가)
- **좌측 사이드바**: 실시간 타임라인 피드. 사이드바 껍데기는 전 페이지 고정이고, 캘린더/히트맵 페이지로 이동 시 내부 콘텐츠만 바뀜(다른 도메인 담당)
- **메인 섹션(시황 탭)**: 8개 시간대 타임라인 콘텐츠. **수집·LLM 생성·DB 저장·조회·스케줄러 구현 완료**
- **메인 섹션(브리핑 탭)**: AI가 당일 시황 데이터로 일간 보고서 작성 → 그 주 일간 보고서를 모아 주간 보고서 작성. **구현 완료(2026-09-13), 사용자 확인 전**

## 브랜치

- `back/feat/timeline` 에서 작업 (main 기준으로 분기)

## 기술 스택

- FastAPI, uv
- DB: Supabase PostgreSQL — SQLAlchemy 비동기 ORM(asyncpg 드라이버)으로 연결. **연동 완료**
  - 연결 문자열은 `.env`의 `DATABASE_URL`로 관리(커밋 금지)
  - Supabase가 주는 문자열은 `postgresql://`로 시작하므로 `postgresql+asyncpg://`로 바꿔서 넣어야 함

---

## 메인 콘텐츠(시황 탭) 데이터 구성

하루 8개 시간대 슬롯으로 구성. 시간대별 화면 명칭은 고정 문구라 프론트에서 매핑한다.

| 시간대 | 명칭 |
|---|---|
| 07:30 | 글로벌 시황 |
| 08:30 | NXT 프리마켓 |
| 09:30 | 장초반 흐름 |
| 12:00 | 오전장 흐름 |
| 14:00 | 오후장 흐름 |
| 15:30 | 장 마감 |
| 17:30 | 애프터마켓 |
| 20:00 | 오늘 시장 분석 |

**모든 슬롯 공통**

1. LLM 브리핑 — 타이틀, 부제 + 요약(타이틀 + 설명) 3개
2. 주린이 해설 — 타이틀, 내용, 태그 3개 (같은 내용을 주식 입문자용으로 쉽게 풀어쓴 것)
3. 주요 뉴스 — 타이틀, 설명, 링크 (네이버 뉴스 API 사용, AI가 주요 뉴스만 선별)
4. 네 번째 칸은 슬롯에 따라 내용이 다르다 (정규장 안/밖)

| 슬롯 | 네 번째 칸 |
|---|---|
| 09:30 / 12:00 / 14:00 / 15:30 | **주도 섹터** — 코스피 업종 등락률 TOP3 |
| 08:30 / 17:30 / 20:00 | **급상승 종목 TOP3** — 종목명·등락률·가격 |
| 07:30 | 없음 (지표 6종이 그 자리를 대신한다) |

- 주도 섹터: 섹터명·등락률 + 섹터 내 상승 1위/거래대금 1위 종목(종목명·등락률).
  상승 1위와 거래대금 1위가 같은 종목이면 "상승·거래 1위"로 합쳐 표기
  **결정(2026-09-14): 상승 1위는 거래대금 필터 없이 순수 등락률 1위로 둔다.** 거래가 거의 없는 종목(09:30 한솔홈데코 14만원, 12:00 유유제약2우B 447만원)이 뽑혀도
  이름과 데이터가 일치하는 쪽을 택했다. 옆의 "거래 1위"가 대표성을 보완한다. 필요해지면 걸러내지 말고 거래대금을 함께 보여주는 방향으로 프런트와 논의
- 급상승 종목: 정규장 밖에는 업종 지수가 산출되지 않아 주도 섹터를 만들 수 없다(실측 확인).
  대신 종목 단위 데이터는 살아 있어서 급상승 TOP3로 대체했다. **상승만 보여주고 하락은 넣지 않는다**
  - 세 슬롯 모두 **넥스트레이드(`market_div_code="NX"`)** 에서 뽑는다.
    08:30은 NXT 프리마켓(08:00~08:50), 17:30 / 20:00은 NXT 애프터마켓(15:40~20:00)
  - **등락률 순위는 `FID_PRC_CLS_CODE="1"`(전일 종가 대비)로 부른다.** `"0"`은 **당일 저가 대비 상승률** 순이라
    화면 등락률(`prdy_ctrt`)과 순서가 다르고, 진짜 상위 종목이 30행 밖으로 밀린다(2026-09-14 실측 — 아래 '겪은 문제').
    받은 뒤에도 `prdy_ctrt`로 한 번 더 정렬해 상위 3개를 쓴다(주도 섹터 상승 1위도 `prdy_ctrt` 최댓값으로 고른다)

**특정 슬롯 전용**

- 07:30 — 어제 마감 & 글로벌 현황: 지표 6종(코스피/코스닥/나스닥/S&P500/니케이/환율)의 명칭·가격·등락폭
- 15:30 — 장중 변화: 코스피/코스닥/환율 3종의 명칭·07:30 가격·마감 가격·변동폭

---

## DB 스키마 (Supabase 생성 완료)

```
timeline_slot ──┬── timeline_briefing_insight    LLM 요약 3행
                ├── timeline_beginner_guide      주린이 해설 3행
                ├── timeline_news                뉴스 5~8행
                ├── timeline_indicator           지표 6종 (07:30 슬롯만)
                ├── timeline_intraday_change     장중 변화 3종 (15:30 슬롯만)
                ├── timeline_top_gainer          급상승 종목 3행 (08:30 / 17:30 / 20:00)
                └── timeline_leading_sector      섹터 3행 (09:30 / 12:00 / 14:00 / 15:30)
                        └── timeline_leading_sector_stock   종목 1~2행
```

- 테이블 9개. 하루에 `timeline_slot` 8행이 쌓이고, 나머지 테이블은 슬롯 id를 FK로 물고 붙는다
- 모든 테이블에 `created_at`이 있고 **한국 시간(naive KST)으로 저장한다.** 파이썬 기본값과
  DB `DEFAULT (now() AT TIME ZONE 'Asia/Seoul')`을 둘 다 걸어뒀다(한쪽만 있으면 서버가
  옛 코드일 때 NOT NULL 위반이 난다 — 실제로 슬롯 3개를 날린 적이 있다)
- `trade_date` + `time_slot` 조합에 유니크 제약 (같은 날 같은 시간대 중복 저장 방지)
- 하루 전체 약 217행 / 연간 약 5만 행 수준 — 용량·성능상 문제 없음
- 브리핑 탭(일간/주간 보고서)은 슬롯에 속하지 않으므로 별도 테이블로 둔다 — 아래 '보고서(브리핑 탭) 설계 — 확정 > 테이블 설계안'

**API 엔드포인트는 테이블 수와 무관하다.** 화면이 하루 단위로 렌더링되므로 `GET /timeline?date=...` 하나로 8슬롯 전체를 중첩 JSON으로 내려주고, 여러 테이블을 읽어 조립하는 것은 services 계층이 담당한다.

---

## 데이터 확보 검증 결과 (실제 호출로 확인 완료)

### 주도 섹터 — KIS API 3개 조합

| 용도 | 엔드포인트 | TR ID |
|---|---|---|
| 업종 목록(코스피 전체) | `/uapi/domestic-stock/v1/quotations/inquire-index-category-price` | `FHPUP02140000` |
| 업종 내 등락률 순위 | `/uapi/domestic-stock/v1/ranking/fluctuation` | `FHPST01700000` |
| 업종 내 거래대금 순위 | `/uapi/domestic-stock/v1/quotations/volume-rank` | `FHPST01710000` |

- 업종 목록에서 등락률 TOP3를 뽑고, 각 업종코드로 종목 순위를 조회하는 방식
- 순위 API 두 개 모두 `FID_INPUT_ISCD`에 업종코드를 넣으면 그 업종 안에서만 순위가 나옴
- 슬롯당 호출 7회(업종 목록 1 + TOP3 x 2), 하루 8슬롯 기준 56회

호출해보고 알게 된 것들:

1. 업종 목록은 등락률 순으로 정렬돼서 오지 않는다. 업종코드 순서로 오므로 정렬은 받는 쪽에서 해야 한다.
2. **주도 섹터는 코스피만 쓴다. 이유는 아래 '코스닥 업종 재검토'에 적었다.**
   (이전 기록 정정: "코스닥은 파생지수만 나오고 실제 업종은 '오락·문화' 하나뿐"이라고 적었는데
   틀렸다. 실제 업종 21개가 다 있다. 못 쓰는 이유는 응답이 불안정한 것이다.)
3. 코스피 38행에도 업종 아닌 것이 섞여 있다. `종합`/`대형주`/`중형주`/`소형주`, `제조`(대분류), `VKOSPI`(변동성 지수), 각종 전략지수. 걸러내지 않으면 "주도 섹터 1위: 종합"이 나온다. 쓸 업종 21개는 `leading_sector_service.py`의 `KOSPI_SECTOR_CODES`에 화이트리스트로 고정.
4. `금융`(0021)은 `증권`(0024)·`보험`(0025)의 상위 분류다. 금융 업종의 종목 순위를 조회하면 증권사·보험사·지주회사가 같이 나온다. KRX 업종분류가 통계청 표준산업분류를 따르기 때문(금융업 아래 은행/증권/보험/기타금융). 셋 다 넣으면 TOP3에 금융과 증권이 나란히 떠서 같은 얘기를 두 번 하게 되므로 증권·보험을 뺐다.
5. 시장 전체가 빠지는 날은 TOP3가 모두 마이너스로 나온다. 정상이며 세시반도 같은 방식으로 표시한다(하락장에서 '주도 섹터'에 마이너스 값 노출 확인). 부호 처리는 프런트에서 `change_rate`가 0보다 큰지 작은지로 나눈다.

### 코스닥은 쓰지 않는다 — 재검토 결과 (2026-09-12)

"인버스 같은 걸 걸러내고 섹터만 뽑아 코스피·코스닥을 합치면 어떤가"를 확인했다.
**결론: 기술적으로 가능하지만 코스피만 쓴다.** 근거 네 가지다.

**① 코스닥 업종 목록이 호출마다 바뀐다 (결정적)**

`FID_MRKT_CLS_CODE=Q` / `FID_INPUT_ISCD=1001`로 조회하면 **항상 정확히 100행**이 오는데,
그 100행의 구성이 매번 다르다. 5회 연속 호출한 결과다.

```
1~3회  100행 중 코스닥 업종  0개
4회    100행 중 코스닥 업종  3개
5회    100행 중 코스닥 업종 13개
       5회 합집합 16개 / 교집합 0개   <- 5회 모두에 나온 업종이 하나도 없다

대조군: 코스피(K/0001) 3회 -> 38행 매번 동일
```

100행 상한에 걸려 잘리는데 **잘리는 지점이 움직인다.** `tr_cont`가 비어 있어 연속조회도 없다.
목록이 매번 달라지면 TOP3 순위를 신뢰할 수 없다.

**② 우회로는 있지만 호출이 4배**

업종코드를 상수로 박고 `get_domestic_index_price("U", 코드)`로 하나씩 찌르면 **26개 전부 조회된다**
(등락률·가격 정상, 종목 순위도 정상 — `1028` -> 앤씨앤/우리기술 실측). 하지만 목록 1회로 끝나는
코스피와 달리 업종마다 1회씩 필요하다.

```
지금    슬롯당  7회      합치면  슬롯당 28회
```

KIS는 초당 한도를 넘기면 HTTP 500 + `EGW00201`을 준다. 지금도 7회 연속에서 걸린 적이 있다.

**③ 업종명 21개 중 19개가 겹친다**

개별 지수 조회는 `hts_kor_isnm`이 **빈 값**으로 와서 이름을 상수로 들고 있어야 한다
(이름을 모으려고 목록 API를 8번 불러 합집합을 만들었다). 그런데 이름이 이렇게 겹친다.

```
겹치는 19개  일반서비스 건설 유통 운송·창고 금융 오락·문화 음식료·담배 섬유·의류 종이·목재
             화학 제약 비금속 금속 기계·장비 전기·전자 의료·정밀기기 운송장비·부품 통신 IT 서비스
코스닥에만   출판·매체복제, 기타제조      코스피에만   전기·가스, 부동산
```

"주도 섹터: 금융 +1.79%"가 어느 시장인지 알 수 없다. 오늘 금융은 코스피 +1.79% / 코스닥 -2.11%로
**부호가 반대**였다. 모든 섹터명에 시장 이름을 붙여야 하고, 그러면 화면 스펙이 바뀐다.

**④ 합치면 코스닥이 TOP3를 잠식한다**

```
등락률 폭   코스피 4.56%p / 코스닥 5.99%p

합친 TOP3   코스닥 출판·매체복제 +2.01%  /  코스피 건설 +1.90%  /  코스닥 오락·문화 +1.50%
            -> 3자리 중 2자리가 코스닥
```

시가총액이 작고 변동성이 큰 코스닥 업종이 상위를 채우면 **"주도 섹터"가 시장을 대표하지 못한다.**
합치는 목적 자체가 흔들린다.

**대안이 필요해지면** 합치지 말고 코스피 TOP3 / 코스닥 TOP3를 따로 보여주는 쪽이 낫다.
이름 중복과 잠식 문제가 둘 다 사라진다. 지표 바에 코스닥 종합이 이미 있어서 화면에서 빠지는 것도 아니다.

쓸 수 있는 코스닥 업종코드 (쓰게 되면 그대로 상수로 박으면 된다):

```
1006 일반서비스  1010 건설      1011 유통      1013 운송·창고   1014 금융
1015 오락·문화   1019 음식료·담배 1020 섬유·의류  1021 종이·목재   1022 출판·매체복제
1023 화학       1024 제약      1025 비금속    1026 금속       1027 기계·장비
1028 전기·전자   1029 의료·정밀기기 1030 운송장비·부품 1031 기타제조  1032 통신
1033 IT 서비스

제외: 1001 KOSDAQ(종합) / 1002~1004 대형·중형·소형주 / 1009 제조(대분류)
      1042~1045 우량·벤처·중견·기술성장기업 / 1049 코스닥 글로벌
```

참고로 레퍼런스인 세시반의 "업종" 카드는 `핸드셋` / `소프트웨어` / `조선`처럼 더 세분화된
분류를 쓴다(FnGuide 계열). KIS는 KRX 표준 업종분류만 주므로 우리 카드는 `전기·전자` /
`기계·장비`처럼 더 큰 단위다. 화면 구성이 같아도 업종명의 세밀함은 다를 수밖에 없다.

### 뉴스 — 네이버 뉴스 검색 API

- 제공 필드: `title` / `description` / `link` / `originallink` / `pubDate`. 언론사명·썸네일은 없음
- **검색어가 필수다.** "이 시간대 주요 뉴스"를 주는 API가 아니라, 우리가 넣은 검색어에 맞는 기사만 준다. 그래서 슬롯별 검색어를 `news_service.py`의 `_SLOT_KEYWORDS`에 표로 정해뒀다
- **정렬은 반드시 `sort="sim"`(정확도순).** 기본값 `date`(최신순)는 관련도를 거의 안 봐서 '시간외 거래'로 검색하면 게임 기사가, '시황'으로 검색하면 증권사 신제품 기사가 나온다
- **슬롯마다 그 시간대에 나온 기사만 담는다.** 모든 슬롯을 "최근 24시간"으로 두면 09:30과 12:00 슬롯이 10건 중 9건 같은 기사를 보여준다(실측).
- **구간은 `[직전 슬롯, 이 슬롯)`** — 시작은 포함, 끝은 배타다(07:30만 전날 20:00부터 — 밤사이 해외장 기사를 담아야 해서). 양쪽을 포함으로 두면 슬롯 시각 정각 기사가 두 슬롯에 중복됐다(08:30:00 기사가 08:30 슬롯과 09:30 슬롯에 둘 다). pubDate가 분 단위라서(표본 400건 전부 초가 `00`) 실제로는 이렇게 잘린다.

  ```
  07:30  전날 20:00 ~ 07:29     08:30  07:30 ~ 08:29
  09:30  08:30 ~ 09:29          12:00  09:30 ~ 11:59
  14:00  12:00 ~ 13:59          15:30  14:00 ~ 15:29
  17:30  15:30 ~ 17:29          20:00  17:30 ~ 19:59
  ```
- **무엇을 남길지는 최신순이 아니라 중요도 점수로 정한다.** 07:30은 구간이 11시간이 넘어서, 최신순으로 자르면 밤사이 나온 중요한 기사가 아침의 자잘한 기사에 밀려 잘려나간다. 점수는 검색어별 정확도 순위를 `1/(60+순위)`로 바꿔 합산한 값이다. 여러 검색어에 동시에 걸린 기사일수록, 각 검색어에서 위에 있을수록 높아진다. 남길 것을 고른 뒤 화면 순서는 최신순으로 되돌린다
- 이 점수는 순위를 합칠 뿐 내용의 중요도를 판단하지는 못한다. 그래서 점수 상위 후보(30건, 07:30은 60건)를 LLM에 넘겨 실을 기사를 고르게 한다(`prompts.NEWS_SELECT_PROMPT`). LLM이 실패하면 점수 상위로 대체
- `link`와 `originallink`는 같은 값이 아니다. `originallink`는 항상 언론사 원문이고, `link`는 네이버 제휴 기사면 `n.news.naver.com` 주소, 아니면 원문 주소다. 60건 표본에서 네이버 도메인은 25%였다. **`link`를 쓰되 없으면 `originallink`로 넘긴다**
- 검색어 고르는 기준: 단어가 짧고 하나일수록 잘 맞는다. `개장시황`/`마감시황`처럼 기사 제목 말머리로 실제 쓰이는 말이 정확도가 높다. `환율`처럼 일상어이기도 한 단어는 제품 가격 기사를 물어오므로 `원달러 환율`로 좁힌다
- 슬롯당 **최소 5건 / 최대 8건**을 싣는다(`news_service._PICK_MIN` / `_PICK_MAX`).
  상한은 처음 10건이었는데 화면 스크롤이 길어져서 8로 줄였다(2026-09-12).
  `timeline_service._NEWS_LIMIT`이 `_PICK_MAX`를 그대로 가져다 쓰므로 고칠 곳은 한 군데다
- 호출량은 슬롯당 검색어 5~6개 x 8슬롯 = 하루 40~48회. 한도 25,000회 대비 여유가 크다

### 지표 6종

지표 바에서 이미 10분마다 수집 중인 값을 그대로 쓴다.

---

## 정규장 밖 데이터 (07:30 / 08:30 / 17:30 / 20:00 슬롯)

결론부터. **업종 지수는 정규장에만 돌고, 종목 데이터는 정규장 밖에도 산다.**
그래서 이 네 슬롯은 주도 섹터를 만들 수 없고, 대신 급상승 종목 TOP3를 쓴다.

### 업종 지수는 정규장 밖에 멈춘다 (실측)

```
업종 목록 API(FHPUP02140000)  시장구분코드 U만 받는다
                              NX(넥스트레이드) / UN(통합) / J는 전부
                              ERROR INVALID FID_COND_MRKT_DIV_CODE

애프터마켓  2026-09-10 23:36 조회 -> 코스피 마지막 체결이 15:32:00
            KRX 시간외단일가(16:00~18:00)를 다 지나서 조회했는데도 마감에 멈춰 있다
프리마켓    2026-09-11 07:46(프리마켓 전)과 08:56(프리마켓 후) 두 번
            둘 다 마지막 체결 없음, 업종 21개 등락률 전부 0.00%로 동일
```

거래소가 업종 지수를 정규장에만 산출하기 때문이지 API 한계가 아니다.

주의: 업종 시간별지수(`FHPUP02110200`)는 체결이 없는 자리를 `99:99:99` 같은 더미 값으로 채워
보낸다. **시(hour) 자리가 00~23이 아닌 행은 걸러야 한다**(분봉에서 본 `88:88`과 같은 현상).
그리고 지금 시점부터 거슬러 최근 100틱만 주므로 **과거를 소급 조회할 수 없다.**

### 종목 데이터는 넥스트레이드로 조회된다

2026-09-11 정규장 중에 시장구분코드를 바꿔가며 확인했다. 넥스트레이드는 정규장에도 같이
거래되므로, 값이 다르게 나오면 실제로 따로 조회된다는 뜻이다.

```
주식현재가     삼성전자   J=5,756,692주  NX=4,329,410주  UN=10,086,114주  <- UN = J + NX
등락률 순위    전기·전자  J=삼화콘덴서 21.18%      NX=솔루스첨단소재 16.40%
거래대금 순위  전기·전자  J=SK하이닉스 1,161,734주 NX=SK하이닉스 760,290주
업종 지수                U만 정상
```

**이 결과로 급상승 종목 TOP3를 구현했다**(`top_gainer_service.py`). 08:30은 NXT 프리마켓,
17:30 / 20:00은 NXT 애프터마켓에서 뽑는다.

(이전 기록 정정: 2026-09-10에 "J와 NX 결과가 같아서 NXT가 무시되는 것 같다"고 적었는데 틀렸다.
그날은 장 마감 후라 NXT도 멈춰 있어서 같아 보였던 것이다. **시장구분 파라미터는 그 값이
유효한 시간대에 테스트해야 한다.**)

### 안 쓰기로 한 것들 (응답은 확인함)

급상승 종목을 NXT 등락률 순위로 만들기로 해서 아래는 쓰지 않는다. 나중에 필요해지면 여기서 시작한다.

| API | TR ID | 비고 |
|---|---|---|
| 예상체결 상승/하락 순위 | `FHPST01820000` | 30행. 08:30 대체 수단으로 코드에 남겨둠 |
| 국내주식 시간외현재가 | `FHPST02300000` | 응답 확인 |
| 국내주식 시간외호가 | `FHPST02300400` | 응답 확인 |
| 주식현재가 시간외일자별주가 | `FHPST02320000` | 최근 30거래일 시간외 종가·거래량 |
| 예상체결가 추이 | `FHPST01810000` | 응답 확인 |

**KRX 시간외 데이터는 종목 편차가 크다.** 시간외일자별주가로 6개를 확인해보니 카카오만 거래량이
잡히고(최근 5거래일 2,967~14,408주) 삼성전자·SK하이닉스·현대차는 30거래일 내내 0이었다.
NXT를 쓰는 이유가 이것이다.

한국거래소 애프터마켓이 2026-09-14부터 오후 4~8시로 열린다는 보도가 있다(미확인).
열리면 `UN`(통합) 값이 달라질 수 있으므로 17:30 슬롯을 재확인할 것.

---

## 파일 구조

```
backend/
├── main.py                     FastAPI 진입점 (라우터 등록, CORS, 스케줄러 9개 작업)
├── create_tables.py            Supabase 테이블 생성용 (없는 테이블만 만든다)
├── logs/timeline.log           로그 (git에 올리지 않음. 14일치 보관)
└── src/backend/
    ├── core/                   전 도메인 공용 (팀 규칙)
    │   ├── config.py             .env 값 로드
    │   ├── logging_config.py     로그 설정 (터미널 + logs/timeline.log)
    │   ├── database.py           Supabase 연결 (get_engine / get_session_factory / Base / get_db)
    │   ├── kis_client.py         한국투자증권 API 호출 (지수·환율·업종·종목순위·휴장일·예상체결·투자자 매매동향·기간별 시세·종목 마스터 파일)
    │   ├── naver_client.py       네이버 뉴스 검색 API 호출 (NCP)
    │   ├── llm_client.py         제미나이 API 호출 (모델 gemini-3.5-flash-lite)
    │   ├── image_client.py       Cloudflare Workers AI 이미지 생성 (보고서 이미지)
    │   └── storage_client.py     Supabase Storage 업로드 (보고서 이미지)
    └── domain/timeline/
        ├── models/timeline.py            타임라인 콘텐츠 9개 테이블
        ├── models/report.py              일간·주간 보고서 7개 테이블
        ├── routers/timeline.py           GET /timeline, /indicators, /glossary, /report + POST /collect/{key}, /report/daily, /report/weekly
        ├── schemas/market_indicator.py   지표 바 응답 형태
        ├── schemas/timeline.py           슬롯 응답 형태 (DTO)
        ├── schemas/report.py             보고서 응답 형태 (DTO)
        ├── services/market_hours.py      지표별 장 운영시간 + 개장일 판단
        ├── services/market_indicator_service.py   지표 바 캐시 관리 + 가공
        ├── services/leading_sector_service.py     주도 섹터 수집 (업종 화이트리스트 포함)
        ├── services/news_service.py               뉴스 수집·정제 (슬롯별 검색어·구간 포함)
        ├── services/briefing_service.py           LLM 브리핑·불개미 해설 생성
        ├── services/prompts.py                    프롬프트 문구·슬롯 명칭
        ├── services/glossary.py                   주식 입문자용 용어 사전 (프런트 호버용)
        ├── services/top_gainer_service.py         급상승 종목 TOP3 수집 (정규장 밖 슬롯)
        ├── services/timeline_repository.py        슬롯 DB 저장·조회
        ├── services/report_repository.py          보고서 DB 저장(수치·문구·이미지 3단계)·조회
        ├── services/report_data_service.py        일간·주간 보고서 수치 수집 (투자자·VKOSPI·분기 차트·섹터 카드)
        ├── services/report_service.py             보고서 생성 흐름 (문구 LLM·섹터 선택·용어·이미지·주간·스케줄러 진입점·응답 변환)
        └── services/timeline_service.py           슬롯 조립 (시간대 정의 포함)
```

API 호출 파일은 도메인 안에 두지 않고 `core`에 모아 전역으로 쓴다(팀 규칙). 특히 KIS는 인증 토큰이 계좌 단위라 도메인마다 만들면 토큰이 중복 발급된다.

---

## 최상단 지표 바 — 동작 순서

### 휴장일에는 갱신하지 않는다 — 국내 지표만 (2026-09-12)

`is_market_open()`이 국내 지표에 대해 **주말 -> 국내 휴장일 -> 개장 시간** 순으로 본다.
전에는 주말만 봐서 추석 같은 공휴일에도 10분마다 KIS를 불렀다.

| 지표 | 휴장일 판정 |
|---|---|
| 코스피 / 코스닥 | 국내휴장일 조회(`CTCA0903R`)의 `opnd_yn` |
| 니케이 / 나스닥 / S&P500 | 보지 않음 (주말 + 개장 시간만) |
| 환율 | 없음 — 항상 갱신 |

판정 결과(실측):

```
                    kospi  kosdaq  nikkei  nasdaq
금 10:00 국장 중          O       O       O       X
금 23:30 미장 중          X       X       X       O
토 10:00                X       X       X       X
추석(목) 10:00           X       X       O       X
```

국내 판정은 스케줄러가 이미 쓰는 `is_trading_day()`를 그대로 재사용한다. 캐시도 공유하므로
새로 드는 호출이 없다.

### 해외 공휴일은 보지 않는다 (검토 후 제외)

KIS에 해외 개장일을 알 수 있는 API가 있다. **`CTOS5011R` "해외결제일자조회"**
(`/uapi/overseas-stock/v1/quotations/countries-holiday`). 개장 여부 필드는 없지만
**거래가 있는 시장만 행으로 오는** 성질을 쓰면 판정이 된다. 실측으로 확인까지 했다.

```
2026-09-11(금)  17행  US JP HK CN GB VN     평범한 개장일
2026-09-12(토)   0행                       주말
2026-09-23(수)  16행  US HK CN GB VN        일본 추분 -> JP만 빠진다
2026-09-02(수)  14행  US JP HK CN GB        베트남 국경일 -> VN만 빠진다
```

**그런데 붙이지 않기로 했다.** 한 번 구현했다가 되돌렸다. 이유는 이득이 비용에 비해 작다는 것이다.

```
아끼는 호출    연간 약 1,000회 = 하루 3회
              (미국 9일 + 일본 17일, 10분 주기 x 장 운영시간)
드는 비용      1. 조회 범위가 당월 말까지다. 2026-09-12에 재보니 9/30까지는 정상이고
                 10/01부터 평일도 전부 0행이었다
              2. 그래서 0행이 "휴장"인지 "데이터 없음"인지 응답으로 구분되지 않는다.
                 0행을 휴장으로 믿으면 10월 1일에 해외 지표 갱신이 통째로 멈춘다
                 (실제로 그렇게 짰다가 발견해서 고쳤다)
```

KIS 호출 한도는 초당 기준이라 하루 3회는 의미가 없다. 애매한 계약을 가진 API를 하나 더 물고
조용히 멈출 위험을 안는 것보다, 해외 공휴일에 값 안 변하는 호출을 몇 번 더 하는 편이 낫다.

**해외 공휴일에 갱신을 시도해도 화면은 틀어지지 않는다.** KIS가 마지막 종가를 주므로 캐시 값이
그대로 유지된다. 낭비되는 것은 호출 횟수뿐이다.

나중에 호출량이 실제로 문제가 되면 위 API와 실측 결과를 그대로 쓰면 된다.
그때는 **0행을 휴장으로 해석하지 말 것** — 판정 불가로 두고 개장으로 기울여야 한다.

### 갱신 흐름

1. **서버가 켜질 때** (`backend/main.py`)
   - `market_indicator_service.refresh_all(force=True)`를 한 번 실행해서 캐시를 채워둔다(장이 닫혀있어도 무조건 한 번은 채움).
   - `APScheduler`로 10분마다(0/10/20/30/40/50분) `refresh_all()`을 자동으로 다시 실행하도록 예약해둔다.

2. **`refresh_all()`이 실행될 때** (`services/market_indicator_service.py`)
   - `_INDICATOR_DEFS`에 적힌 6개 지표를 하나씩 돈다.
   - 각 지표마다 `market_hours.is_market_open()`으로 시장이 열려있는지 확인한다.
     - 닫혀있으면 → 건너뛴다(캐시의 이전 값 유지 = 장마감 시 값 고정).
     - 열려있으면(달러환율은 항상) → `kis_client.py` 함수를 호출한다.

3. **`kis_client.py`가 KIS 서버에 요청** — `get_access_token()`으로 토큰을 준비(캐시된 토큰 재사용)한 뒤, 국내지수는 `get_domestic_index_price()`, 해외지수·환율은 `get_overseas_index_or_fx_price()` 호출

4. **데이터 가공** (`_refresh_one()`) — 응답에서 현재가·전일대비율만 뽑아 `_cache` 딕셔너리에 저장
   - **국내 지수가 장 밖에서 등락률 0.00으로 오면** `_closed_domestic_change_rate()`가 일자별 지수(`get_index_daily_price` D)에서
     종가가 현재가와 같은 가장 최근 날의 등락률로 바꾼다(개장일 09:00 전에는 오늘 행 제외). 장중 0은 실제 보합이라 그대로 둔다

5. **프론트 요청 시** (`routers/timeline.py`) — `GET /timeline/indicators`는 KIS를 다시 호출하지 않고 `_cache` 값을 스키마 형태로 변환해 응답

정리하면: **KIS 호출은 정시 스케줄러가 백그라운드에서만 하고, 프론트 요청은 항상 캐시된 값을 즉시 받아간다.**

> 프론트도 10분 주기에 맞춰 재요청해야 화면이 갱신된다. 브라우저 탭이 백그라운드이거나 절전 상태면 타이머가 밀릴 수 있음.

---

## 메인 콘텐츠 수집 — 동작 순서

수집·LLM 생성·DB 저장·조회·스케줄러까지 모두 붙어 있다.

### 엔드포인트

| 경로 | 내용 |
|---|---|
| `GET /timeline?date=` | **하루치 조회. 프런트가 쓰는 엔드포인트.** DB에서 읽어 즉시 응답(0.2초) |
| `POST /timeline/collect/{slot_key}` | 슬롯 하나를 수집해서 DB에 저장. 스케줄러가 부를 동작을 수동 실행 |
| `GET /timeline/indicators` | 최상단 지표 바 (캐시에서 읽음) |
| `GET /timeline/glossary` | 용어 사전 `{용어: 설명}` 19개. 프런트가 호버 설명에 쓴다 |
| `GET /timeline/report?type=daily\|weekly&date=` | 보고서 조회 (프런트용). 없으면 `null` — 아래 '보고서' 섹션 |
| `POST /timeline/report/daily?date=` / `POST /timeline/report/weekly?date=` | 보고서 수동 생성 |

`GET /timeline`은 오늘 날짜를 조회하면 아직 시간이 안 된 슬롯을 빼고 준다. 지난 날짜는 전부 내려준다.
슬롯 시각에 수집을 시작하므로 데이터가 시각보다 먼저 생길 일은 없지만, 확인용으로 미래 슬롯을
수동 수집했을 때를 막는 안전망으로 남겨뒀다.

수집·저장에 걸리는 시간은 슬롯당 1~2분이다(실측 78초, LLM 3회 포함). `with_briefing=false`를 붙이면
LLM을 건너뛰어 1~2초에 끝난다.

`slot_key`는 콜론 없이 4자리로 받는다(`0730`, `1200`). URL에 콜론을 쓸 수 없어서다. DB에 저장할 때 쓰는 `07:30` 형태로는 서비스 안에서 변환한다. 시간대 정의(`_SLOT_DEFS`)와 명칭도 여기 있다.

슬롯별로 채워지는 항목이 다르다.

- 주도 섹터: 09:30 / 12:00 / 14:00 / 15:30만 채워지고 나머지는 빈 배열. 정규장 밖에는 업종 지수가 돌지 않는 것을 실측으로 확인했다
- 급상승 종목: 08:30 / 17:30 / 20:00만 채워진다
- 지표 6종: 07:30에만 채워진다. 그 시간에는 환율 빼고 전 시장이 닫혀 있어서 지수 5종은 어제 국내 종가와 밤사이 미국 마감값이고, 슬롯 성격("어제 마감 & 글로벌 현황")과 정확히 맞는다.
  **캐시를 읽지 않고 새로 받는다**(`force_refresh=True`). 환율은 24시간 움직여서 캐시가 최대 10분 낡을 수 있고, 지표 바 갱신이 07:30 정각에 같이 예약돼 있어 순서를 보장할 수 없다. 이 값이 15:30 장중 변화의 기준점이고 놓치면 되살릴 수 없다. 15:30 슬롯도 같은 이유로 새로 받으므로 두 시점을 같은 방식으로 잰다
- 브리핑·주린이 해설: 모든 슬롯. LLM이 실패하면 `briefing_headline`이 `null`, 목록은 빈 배열
- 장중 변화: 15:30만. 그날 07:30 슬롯이 DB에 있어야 만들어진다

### 단계별 소요 시간

수집 함수가 단계마다 걸린 시간을 찍는다(`timeline_service._timed`). 느려지거나 실패했을 때
KIS인지 네이버인지 제미나이인지 총시간만으로는 가릴 수 없어서 넣었다.

```
[수집] 07:30 시작
  [지표 6종] 0.5초
  [뉴스] 1.9초
  [LLM 브리핑·해설] 4.6초
[수집] 07:30 완료 - 총 7.1초
```

**측정값 해석에 주의할 것.** 위는 2026-09-12(토) 측정치로 뉴스가 6건뿐이었다.
거래일에는 뉴스가 30건 가까이 들어가 프롬프트가 몇 배로 길어지고, 그때 실측은 **78초**였다.
짧은 프롬프트로 잰 값을 그대로 믿으면 안 된다(전에 "30초→2초"로 잘못 판단한 적이 있다).

KIS·네이버 수집만 보면 슬롯당 1초 안팎이다(8슬롯 전체 0.5~1.4초). **병목은 전부 LLM이다.**

**주도 섹터** (`services/leading_sector_service.py`)

1. `collect()`가 `kis_client.get_index_category_price("K")`로 코스피 업종 전체를 받는다
2. `KOSPI_SECTOR_CODES`에 있는 업종만 남기고(38행 -> 21행) 등락률 내림차순으로 정렬한다
3. 상위 3개 각각에 대해 `get_fluctuation_ranking()`과 `get_volume_ranking()`을 호출해 1위 종목을 뽑는다
4. 두 API가 같은 종목을 가리키면 "상승·거래 1위"로 한 줄로 합치고, 다르면 "상승 1위" / "거래 1위" 두 줄로 만든다

지표 바와 달리 캐시를 두지 않았다. 슬롯을 만들 때 한 번만 필요한 값이라 조회한 결과를 바로 DB에 넣는 쪽이 맞기 때문이다.

**API 호출 실패 처리**

`kis_client.py`와 `naver_client.py`의 조회 함수는 실패하면 최대 3회까지 다시 시도한다(0.5초부터 조금씩 늘려가며 대기).

- 5xx와 연결 오류는 재시도한다. KIS는 초당 호출 한도를 넘기면 HTTP 500 + `EGW00201`을 주는데(확인 완료), 주도 섹터가 한 번에 7회를 연달아 부르기 때문에 여기 걸릴 수 있다
- 4xx는 요청이나 키가 잘못된 것이라 다시 보내도 결과가 같으므로 바로 에러를 낸다
- 토큰 발급(`get_access_token`)에는 재시도를 적용하지 않았다. 계좌 주인에게 알림이 여러 번 가기 때문이다

**뉴스** (`services/news_service.py`)

1. `collect(time_slot)`이 `_SLOT_KEYWORDS`에서 그 슬롯의 검색어를 꺼낸다
2. 검색어마다 `naver_client.search_news(sort="sim")`으로 30건씩 받는다
3. `<b>` 태그와 HTML 특수문자를 없애고, `pubDate`를 한국 시간 datetime으로 바꾼다
4. 링크 기준으로 합치면서 중요도 점수를 더한다(같은 기사가 여러 검색어에 걸리면 점수가 올라간다)
5. `_SLOT_WINDOW` 구간에 드는 기사만 남긴다. 3건 미만이면 구간을 6시간으로 넓혀 한 번 더 걸러본다
6. 점수 높은 순으로 상위 N건을 고른 뒤, 화면 순서는 최신순으로 되돌려 돌려준다

---

## LLM 연동 (브리핑·불개미 요약)

프롬프트 문구는 전부 `services/prompts.py`에 있다. 결과를 보면서 계속 손보게 되는 값이라
서비스 코드와 분리해뒀다.

### 두 단계로 부른다

1. **브리핑** — 수집 데이터 → 헤드라인 + 부제 + 포인트 3개
2. **불개미 해설** — 수집 데이터 + 브리핑 → 왜 그런지 풀어 설명한 문단 3개

스펙("위 세 데이터를 바탕으로 주린이 해설")에 맞춰 해설도 원본 데이터를 받는다(2026-09-13).
처음에는 브리핑만 넘겼는데, 그러면 브리핑이 한 줄로 넘어간 배경을 해설이 설명할 재료가 없었다.
브리핑과 다른 얘기를 하지 않도록 [데이터]·[브리핑]에 없는 사실은 금지하고, 뉴스에서 온 내용은
출처를 밝히게 했다.

### 슬롯별 LLM 재료 (스펙 대조 완료)

| 슬롯 | 브리핑 재료 | 해설 재료 |
|---|---|---|
| 07:30 | 지표 6종 + 뉴스 | 지표 + 뉴스 + 브리핑 |
| 08:30 | NXT 프리마켓 급상승 TOP3 + 뉴스 | 급상승 + 뉴스 + 브리핑 |
| 09:30 / 12:00 / 14:00 | 코스피 업종 TOP3 + 뉴스 | 업종 + 뉴스 + 브리핑 |
| 15:30 | 장중 변화 + 업종 TOP3 + 뉴스 | 장중 변화 + 업종 + 뉴스 + 브리핑 |
| 17:30 / 20:00 | NXT 애프터마켓 급상승 TOP3 + 뉴스 | 급상승 + 뉴스 + 브리핑 |

**`prompts.SLOT_FOCUS`에도 슬롯별 재료를 문장으로 적어뒀다.** 데이터만 넘기고 그게 무엇인지 안
알려주면 LLM이 급상승 종목을 정규장 결과처럼 쓸 수 있다. 재료를 명시한 뒤 17:30 브리핑이
"넥스트레이드 애프터마켓 동향"으로 정확히 짚는 것을 확인했다. **슬롯에 넣는 데이터를 바꾸면
`SLOT_FOCUS` 문장도 같이 고칠 것.**

뉴스 선별도 LLM이 한다(`news_service`). 그래서 슬롯당 LLM 호출은 3회다.

### 모델 — gemini-3.5-flash-lite

`gemini-3.6-flash`에서 바꿨다. 이유는 한도 하나다.

- **무료 한도**: 3.6-flash는 하루 20회다(429 응답의 `GenerateRequestsPerDayPerProjectPerModel-FreeTier`).
  슬롯당 3회 x 8슬롯 = 24회라 한도를 넘어서, 실제로 09:30 이후 슬롯 브리핑이 통째로 비었다.
  Lite는 하루 200회다.
- **속도**: 짧은 프롬프트로 재보면 30초에서 2초로 줄지만, 실제 브리핑 프롬프트는 뉴스 30건이
  들어가서 훨씬 길다. 슬롯 하나를 끝까지 만드는 시간은 실측 78초로, 교체 전(72~99초)과 큰 차이가 없다.
  **속도는 교체 이유가 되지 못한다. 실제 이유는 한도다.**

한도는 모델별로 따로 잡힌다. 한 모델이 429가 나도 다른 모델은 쓸 수 있다.

Lite로도 한도가 부족하거나 품질이 안 나오면, LLM 호출 3회를 1회로 합치는 방법이 있다
(후보 뉴스를 주고 "고를 번호 + 브리핑 + 불개미 요약"을 한 응답으로 받는 방식, 하루 8회).
테이블 구조는 그대로 두고 호출 방식만 바뀐다. 지금은 보류.

### 프롬프트에서 신경 쓴 것

- **숫자를 지어내지 못하게 막았다.** 입력을 `[확정 수치]`(시세 API에서 받은 값)와 `[뉴스]`로 나눠서
  넘긴다. 유가·금·선물은 계좌 권한이 없어 시세로 못 가져오므로 뉴스에서만 나오는데,
  기사 발행 시각을 같이 주고 "지금 값인 것처럼 쓰지 말고 출처를 밝혀라"를 규칙으로 넣었다.
  실제 결과에서 `기사에 따르면 브렌트유가 배럴당 100달러를 넘어서며`처럼 나온다.
- **투자 권유를 금지했다.** 화면에 "최종 투자 결정과 책임은 투자자 본인에게 있습니다" 안내가 붙는데,
  그러려면 본문이 실제로 권유를 안 해야 한다.
- **문체를 못박았다.** 규칙이 없으면 모델 성향대로 나온다. 3.6-flash는 `~했습니다`체,
  Lite는 `~보였다`체로 썼다. 시안에 맞춰 `~습니다`체와 `%` 기호로 고정했다.
- **용어 풀이를 LLM에게 맡기지 않는다.** Lite가 `매도세`를 "판 것보다 산 것이 적은 상태"라고
  뒤집어 쓴 적이 있다(맞는 설명은 그 반대). 입문자용 해설에서 정의가 틀리면 그냥 둘 수 없어서
  19개 용어의 설명을 우리가 정해뒀다.
- **불개미 해설이 브리핑을 베끼지 못하게 했다.** "브리핑과 똑같은 문장이 나오면 잘못한 것"을
  규칙에 넣었다.

### 주린이 해설 — 용어 정의에서 문맥 설명으로 (2026-09-12)

처음에는 용어집을 프롬프트에 넣고 "이 용어가 나오면 괄호로 설명을 붙여라"라고 지시했다.
그랬더니 결과가 해설이 아니라 **용어 사전처럼** 나왔다. 세 문단에 괄호 설명이 대여섯 개 들어가면
정작 "왜 그런 일이 일어났는지"를 쓸 자리가 없다.

해설의 역할은 단어를 정의하는 게 아니라 문맥을 전달하는 것이다. 그래서 지시를 바꿨다.

- 전문 용어에 **괄호로 뜻을 붙이지 못하게 금지**했다
- 대신 그 용어를 몰라도 상황이 이해되게 문장을 쓰게 했다
  (`순매도가 이어졌습니다` -> `외국인이 계속 주식을 팔았습니다`)
- 나쁜 예를 두 개 넣었다. 브리핑을 그대로 옮긴 것, 단어만 정의한 것 둘 다 실패다

바뀐 결과(실측):

```
[국내 주가와 환율이 내린 이유]
국내 시장에서 주가가 내린 것은 투자자들이 안전한 현금을 선호하며 주식을 팔았기 때문입니다.
환율이 내린 것은 원화의 가치가 전날보다 조금 올랐음을 뜻합니다.
```

**용어 사전은 버리지 않고 `services/glossary.py`로 옮겼다.** LLM이 만든 정의가 아니라
우리가 검수한 19개라 그대로 쓸 수 있다. `GET /timeline/glossary`로 내려가고,
**프런트가 본문의 용어에 마우스를 올리면 설명이 뜨는 방식으로 처리한다.**
이렇게 하면 아는 사람은 그냥 읽고 모르는 사람만 확인하면 된다.

용어를 늘리거나 설명을 다듬으려면 `glossary.py`의 표만 고치면 된다.
주식 잘 아는 사람에게 검토받을 때 이 표를 같이 볼 것.

### 실패해도 슬롯이 죽지 않는다

LLM이 실패하면 경고만 찍고 나머지는 정상 저장된다.

- 뉴스 선별 실패 → 점수 상위 N건으로 대체
- 브리핑 실패 → `briefing_headline`이 `None`. 지표·뉴스·주도 섹터는 그대로 나간다
- 불개미 해설은 브리핑도 재료로 쓰므로, 브리핑이 없으면 빈 배열

저장된 결과는 `GET /timeline?date=`로 확인한다. (저장 없이 즉석 수집하던 `GET /timeline/slot/{key}`는 9/14 삭제 — 부를 때마다 KIS·제미나이 3회를 쓰는 인증 없는 GET이었다. 저장하지 않고 LLM 결과를 보려면 scratchpad 스크립트에서 `timeline_service.collect_slot`을 직접 부른다)

---

## DB 저장·조회

`services/timeline_repository.py`가 DB 접근을 전담한다. 조립(`timeline_service`)과 나눈 이유는
수집·가공 로직과 DB 접근이 한 파일에 섞이면 저장 방식만 바꾸고 싶을 때 건드릴 범위가 넓어지기 때문이다.

### 재실행 안전하게 만든 두 가지

**같은 슬롯을 다시 돌리면 덮어쓴다.** `trade_date` + `time_slot`에 유니크 제약이 있어서 그냥
INSERT하면 두 번째 실행부터 에러가 난다. 테스트 반복, 수집 실패 후 재시도, 서버 재시작 모두
재실행이 필요하다.

**새로 만든 게 비었으면 기존 값을 유지한다.** 브리핑이 잘 들어간 슬롯을 지표 때문에 다시 수집했다가
그 순간 LLM 한도에 걸려서 멀쩡했던 브리핑이 빈 값으로 덮인 일이 있었다. 지금은 새 브리핑이 없으면
기존 브리핑과 불개미 요약을 살려둔다.

### 비동기 ORM에서 주의할 점

조회할 때 `selectinload`로 자식 테이블을 미리 같이 읽어와야 한다. 동기 ORM처럼 나중에
`slot.news`를 꺼내려 하면 `MissingGreenlet` 에러가 난다. 테이블을 추가하면
`timeline_repository._EAGER_LOAD`에도 넣어야 한다. 빠뜨리면 그 값만 조용히 비어 보인다.

### 장중 변화 (15:30 슬롯)

그날 07:30 슬롯의 지표를 DB에서 읽어 현재 가격과 비교한다. 코스피·코스닥·환율 3종이고
명칭·07:30 가격·현재 가격·변동폭을 담는다.

07:30 슬롯이 없으면 경고를 찍고 빈 목록을 돌려준다. **07:30 시점의 환율은 지나가면 되살릴 수 없다**
(코스피·코스닥은 07:30 값이 곧 전일 종가라 복원 가능하지만 환율은 24시간 움직인다).
그래서 07:30 슬롯 수집이 실패하면 재시도로 반드시 채워두는 것이 중요하다.

---

## 결정된 설계 방향

### 테스트 데이터 지우기

```sql
DELETE FROM timeline_slot;   -- 자식 행이 전부 같이 지워진다
```

ORM 쪽 `cascade="all, delete-orphan"`과 DB 쪽 `ON DELETE CASCADE`를 둘 다 걸어뒀기 때문이다.
두 개를 같이 걸어둔 이유가 있다. `cascade`는 파이썬 코드로 지울 때(`session.delete`)만 동작한다.
Supabase 대시보드에서 SQL을 직접 실행하면 ORM을 거치지 않아서 FK 제약에 막혀
`ForeignKeyViolationError`가 났다.

보고서도 같다. `DELETE FROM timeline_report;`면 자식 6개 테이블이 같이 지워진다.
Storage 이미지(`report-images` 버킷)는 DB와 따로라 대시보드나 API로 따로 지운다.

**`DELETE`는 id 번호를 되돌리지 않는다.** 1번부터 다시 쌓으려면 삭제 후 16개 테이블의 시퀀스를 초기화한다
(`{테이블명}_id_seq`). 2026-09-13 밤 테스트 데이터를 지운 뒤 16개 모두 초기화했다.

```sql
ALTER SEQUENCE timeline_slot_id_seq RESTART WITH 1;
-- timeline_briefing_insight / timeline_beginner_guide / timeline_news / timeline_indicator
-- timeline_intraday_change / timeline_top_gainer / timeline_leading_sector / timeline_leading_sector_stock
-- timeline_report / timeline_report_section / timeline_report_point / timeline_report_quarter
-- timeline_report_sector / timeline_report_keyword / timeline_report_term 도 동일하게
```

### 갱신 타이밍 — 타임라인 시각에 바로 반영한다

세시반을 관찰한 결과, 14:00이 되면 데이터는 바로 들어오지만 **새로고침을 해야 화면에 보인다.**
우리는 그렇게 하지 않고, 정해진 시각에 바로 저장하고 프런트에도 바로 뿌리는 방향으로 간다.

여기서 갈리는 지점이 하나 있다. 백엔드가 제때 저장해도 **브라우저가 다시 요청하지 않으면 화면은 그대로다.**
"새로고침 없이 바로"를 만들려면 프런트 쪽 방식을 정해야 한다.

- 폴링: 프런트가 슬롯 시각에 맞춰 다시 요청한다. 만들기 쉽지만 탭이 백그라운드면 타이머가 밀릴 수 있다
- 서버 푸시(SSE/WebSocket): 백엔드가 저장 직후 프런트로 밀어준다. 정확하지만 구현이 늘어난다

지표 바도 같은 문제를 안고 있다(10분 갱신). 프런트 담당자와 같이 정할 것.

### 스케줄러 수집 시각

슬롯 시각이 되면 그때 수집한다. 생성에 1~2분 걸리므로(실측 78초) 화면에는 그만큼 늦게 뜬다.

**15:30 슬롯만 15:35에 수집하고 나머지는 정시다.** (9/14 실데이터에서 정시 수집이 종가 동시호가 반영 전 값을 저장해 되돌림 — 아래)

| 슬롯 | 07:30 | 08:30 | 09:30 | 12:00 | 14:00 | 15:30 | 17:30 | 20:00 |
|---|---|---|---|---|---|---|---|---|
| 수집 | 07:30 | 08:30 | 09:30 | 12:00 | 14:00 | **15:35** | 17:30 | 20:00 |

### 15:30 슬롯 — 15:35에 수집한다 (2026-09-14 실측)

9/14 15:30:06에 저장된 값이 **15:29 값**이었다. 틱을 보면 종가 동시호가 결과가 `153000` 틱부터 반영된다.
KOSPI 저장 6,681.73(-3.30%) / 실제 종가 6,684.37(-3.26%), KOSDAQ 807.44 / 806.79. 섹터 TOP3 순서도 달랐다
(저장 오락·문화 1.20 > 운송·창고 1.15 > 섬유·의류 0.98 / 마감 섬유·의류 1.47 > 운송·창고 1.18 > 오락·문화 1.14), 운송·창고 상승 1위도 대한항공→아시아나항공.
15:37에 마감값으로 DB(장중 변화 2행, 섹터 3개·종목, 제목·부제·포인트 2개)를 고치고 수집 시각을 옮겼다.
1분 틱으로 반영 시점을 확인: 15:25~15:29는 동시호가라 값이 멈춰 있고, **코스피·섬유·의류는 15:30 틱, 코스닥은 15:32 틱**에 종가가 들어왔다
(운송·창고·오락·문화는 15:30 틱 값도 종가와 0.01~0.06%p 달랐고 15:37 조회 때는 종가와 같았다). 처음엔 최소 지연으로 15:33을 검토했으나, 업종 값이 15:30 틱 뒤에도 바뀐 것을 근거로 사용자와 **안전하게 15:35**로 정했다.
**9/15에 15:30 슬롯 값이 일자별 종가·업종 순위와 일치하는지 확인한다.**
**16:00부터 KRX 애프터마켓이 열려 종목 현재가가 다시 움직이므로 15:35~15:59 사이여야 한다.** 뉴스 구간(`[14:00, 15:30)`)은 슬롯 시각 기준이라 영향 없음.

아래는 9/12에 정시로 바꿨던 당시 기록이다.

### (이전 기록) 15:30 슬롯 — 정시로 통일했다 (2026-09-12)

전에는 15:35였다. 코스피 종가가 15:20~15:30 마감 동시호가로 정해지므로 정시에 조회하면
마감 전 값이 들어갈 수 있다는 우려였다. 정시로 바꾼 이유는 두 가지다.

1. **그 차이가 실제로 있는지 확인된 적이 없다.** 소급 조회가 불가능해서 재볼 수도 없었다
   (지수 시간별 조회는 지금부터 거슬러 최근 100틱만 주고, 과거 시각을 넣으면 무시된다.
   휴장일에 호출하면 0행이 온다). 확인되지 않은 우려 때문에 슬롯 하나만 규칙을 다르게
   유지하는 대가가 더 컸다.
2. **캐시 문제를 이미 고쳤다.** 예전에는 장중 변화가 지표 *캐시*를 읽었는데, 캐시는 10분
   주기로 갱신되고 15:40에는 장마감이라 건너뛴다. 그래서 15:35에 만들어도 캐시에는
   15:30:00 값이 있었다 — **5분을 기다린 효과가 애초에 없었다.** 지금은 이 슬롯만
   `_indicator_rows(force_refresh=True)`로 새로 받는다.

**월요일(9/14) 실데이터에서 확인할 것.** 15:30 슬롯의 `closing_price`와 실제 종가를 비교한다.
다르면 `SLOT_COLLECT_TIMES`의 `"1530"`만 `(15, 35)`로 되돌리면 된다.

처음에는 **5분 전에 미리 만들어두는 방식**이었는데 바꿨다. 정각에 화면에 띄우려던 의도였지만
근거가 약했다.

- 데이터가 슬롯 시각과 어긋난다. "12:00 오전장 흐름"에 11:55 값이 들어간다. 화면 노출은
  지나가면 끝이지만 **저장된 데이터는 영구히 남는다.**
- 뉴스 구간이 잘린다. 12:00 슬롯의 구간은 09:30~12:00인데 11:55에 수집하면 마지막 5분이 빠진다.
- 07:30 환율이 실제로는 07:25 값이 되어, 15:30 장중 변화가 그 값을 기준으로 계산된다.
- 미리 만드는 방식은 **마감이 있다.** 07:25에 실패하면 07:30까지 5분 안에 성공해야 한다.
  정시 수집은 마감이 없어서 실패해도 다시 시도하고 조금 늦게 올리면 된다.
- 15:30을 이미 정확성 때문에 뒤로 미뤄뒀는데, 나머지 7개만 정각 노출을 우선하는 건 앞뒤가 안 맞았다.

대가는 슬롯이 1~3분 늦게 뜬다는 것인데, 하루 8번 갱신되는 타임라인에서 정각 노출은 아무도
체감하지 못한다. 레퍼런스인 세시반도 새로고침해야 보인다.

시각을 바꾸려면 `timeline_service.SLOT_COLLECT_TIMES` 표만 고치면 된다.

### 프런트는 어떻게 데이터가 준비된 걸 아는가

별도 확인 방법이 필요 없다. `GET /timeline`이 **DB에 있는 슬롯만** 배열로 주기 때문이다.

```
09:35에 요청 -> [07:30, 08:30, 09:30]        3개
12:00에 요청 -> [07:30, 08:30, 09:30]        3개  (12:00은 아직 생성 중)
12:03에 요청 -> [07:30, 08:30, 09:30, 12:00] 4개
```

프런트는 주기적으로(1분 정도) 다시 부르고, 배열에 새 슬롯이 생겼으면 화면에 붙이면 된다.
DB에서 읽기만 해서 0.2초에 응답하므로 1분 폴링은 부담이 없다.

"생성 중"을 화면에 표시하고 싶으면 프런트에서 구분할 수 있다. 프런트가 고정으로 가진 슬롯 시각이 지났는데
`GET /timeline` 응답에 없으면 생성 중이다. 백엔드 수정은 필요 없다. (슬롯 목록 `GET /timeline/slots`는 명칭을 프런트가 매핑하기로 해서 9/14 삭제)

나중에 폴링이 마음에 안 들면 SSE로 올릴 수 있다. 폴링으로 시작하고 필요할 때 바꾼다.

### 휴장일에는 아무것도 하지 않는다 (실측 확인)

주말과 공휴일에는 슬롯을 만들지 않는다. 레퍼런스인 세시반도 그날은 데이터를 제공하지 않는다.

판단은 국내휴장일 조회(`CTCA0903R`)를 쓴다. 응답의 `opnd_yn` 한 필드로 주말이든 공휴일이든
장이 안 서는 날이 전부 `N`으로 온다. 우리가 요일을 따지거나 공휴일 목록을 관리할 필요가 없고,
임시공휴일·대체공휴일도 거래소가 반영한 대로 따라간다. 한 번에 20여 일치가 오므로
`market_hours._open_day_cache`에 담아 재사용한다.

```
9/11(금) O   9/12(토) X   9/13(일) X   9/14(월) O
9/24(목) X   9/25(금) X   9/28(월) O        <- 추석도 정상 감지
```

**어디까지 막는가** — 호출 단위로 세어 확인했다.

| 대상 | 장마감 | 국내 휴장일 | 해외 휴장일 | 주말 |
|---|---|---|---|---|
| 코스피·코스닥 | 호출 X | **호출 X** | (해당 없음) | 호출 X |
| 니케이·나스닥·S&P500 | 호출 X | 호출 O | 호출 O (한계) | 호출 X |
| 환율 | 항상 호출 | 항상 호출 | 항상 호출 | 항상 호출 |
| 타임라인 슬롯 | — | **전부 호출 X** | (국내 기준만 봄) | 전부 호출 X |

```
지표 바 (10분 주기)                   실제 호출된 지표
  금 10:00  국장 중          코스피, 코스닥, 환율, 니케이     4회
  금 17:00  국장 마감 후      환율                        1회
  금 23:30  미장 중          나스닥, S&P500, 환율          3회
  추석(목) 10:00  국장 휴장   환율, 니케이                  2회   <- 국내만 빠진다
  토 10:00  주말            환율                        1회

타임라인 슬롯 (휴장일에 8슬롯 전부 호출해봄)
  KIS 0회 / 네이버 0회 / 제미나이 0회      <- 외부 호출이 아예 없다
```

슬롯 수집은 `is_trading_day()` 확인이 **KIS·네이버·제미나이 호출과 DB 접근보다 앞**에 있어서
휴장일에는 수집 자체가 시작되지 않는다.

**조회에 실패하면 개장일로 본다.** 휴장일에 괜히 수집하는 건 데이터를 지우면 되지만, 개장일인데
건너뛰면 그 슬롯을 영영 못 채운다. 특히 07:30 환율은 그 시점을 놓치면 되살릴 방법이 없다.

예외가 하나 있다. **서버가 켜질 때는 `refresh_all(force=True)`로 6종을 무조건 한 번 채운다.**
캐시가 비면 지표 바가 아무것도 못 내려주기 때문이다(장이 닫혀 있어도 마지막 종가는 보여줘야 한다).

### AsyncIOScheduler를 쓴다

`BackgroundScheduler`에서 바꿨다. 슬롯 수집 함수가 `async def`인데, `BackgroundScheduler`는 별도
스레드에서 돌아서 비동기 함수를 실행할 수 없다. **등록해두면 에러도 안 나고 아무 일도 안 일어난다.**
로그에는 작업을 실행했다고 찍히는데 DB에는 아무것도 없는 상태가 된다.

`AsyncIOScheduler`는 FastAPI가 이미 돌리고 있는 이벤트 루프 위에서 돌아 `async` 함수를 제대로
실행한다. 평범한 함수(지표 바 갱신)도 그대로 받아준다.

주의: 이벤트 루프가 있어야 도므로 **서버를 띄웠을 때만 동작한다.** 스크립트로 따로 실행하면 안 돈다.

등록되는 작업은 9개다(지표 바 1 + 슬롯 8). `DATABASE_URL`이 없으면 슬롯 작업은 등록하지 않고
경고만 남긴다. KIS 키가 없으면 지표 바 작업을 등록하지 않는다.
보고서는 따로 예약하지 않는다. 20:00 슬롯 작업(`run_scheduled_collect("2000")`)이 끝나면 같은 작업 안에서
`report_service.run_scheduled_reports()`를 이어서 부른다.

`misfire_grace_time`을 10초로 뒀다. 기본값 1초는 너무 빡빡해서 서버가 잠깐 버벅이기만 해도
그 슬롯이 통째로 날아간다. 반대로 길게 잡으면 안 된다. 맥이 잠들었다 몇 시간 뒤에 깨어났을 때
07:30 슬롯이 09시에 뒤늦게 실행되면 그때 환율이 "07:30 가격"으로 저장되어 장중 변화가 틀어진다.
**없는 것보다 틀린 게 나쁘다.**

서버를 밤새 켜두려면 맥이 잠들지 않아야 한다. `caffeinate -d uv run fastapi run main.py`로
띄우면 서버가 도는 동안만 잠자기가 막힌다. `fastapi dev`는 파일이 바뀌면 서버를 재시작하므로
장시간 운영에는 `run`이 안전하다.

### 오류 로깅 (2026-09-12 구현)

**`print`를 전부 `logging`으로 바꿨다(16곳).** 설정은 `core/logging_config.py`에 있다.

바꾼 이유는 하나다. **print는 터미널에만 남아서 서버를 닫으면 사라진다.**
2026-09-11에 슬롯 3개가 저장 직전에 실패했는데 터미널이 이미 지나가서 원인을 찾는 데 한참 걸렸다.
실데이터를 쌓기 시작하면 아침에 일어나서 "07:30이 왜 비었는지"를 물어야 하는데, 그때 볼 게 있어야 한다.

**어디에 남는가**

```
1. 터미널                      전에 print로 보던 것과 같다
2. backend/logs/timeline.log   자정에 날짜를 붙여 넘기고 14일치 보관
                               (timeline.log.2026-09-14 형태)
```

`logs/`는 루트 `.gitignore`에 넣었다.

**레벨 기준**

| 레벨 | 언제 | 예 |
|---|---|---|
| INFO | 정상 흐름 | 수집 시작·완료, 단계별 소요 시간, 휴장일 건너뜀, 기존 브리핑 유지 |
| WARNING | 일부 실패했지만 슬롯은 만들어짐 | LLM 실패, 뉴스 선별 실패, 휴장일 조회 실패, 07:30 없어서 장중 변화 못 만듦 |
| ERROR | 슬롯·보고서가 통째로 실패 | 수집 실패, 보고서 생성 실패. `exc_info=True`로 스택까지 남긴다 |

운영 중에는 이 한 줄만 보면 된다.

```
grep -E "WARNING|ERROR" logs/timeline.log
```

**외부 로거는 조용하게 낮췄다.**

```
httpx / httpcore   요청마다 INFO 한 줄. 슬롯 하나에 수십 번 부른다
apscheduler        작업 등록마다 두 줄. 작업이 9개라 서버를 켤 때마다 18줄
                   (실측: 낮추기 전 23줄 중 19줄이 이것이었다 -> 3줄로 줄었다)
```

둘 다 **WARNING 이상은 남겨뒀다.** 특히 apscheduler의 경고는 꼭 봐야 한다 —
예약 시각을 놓쳤을 때(`Run time of job ... was missed by ...`) 여기 찍힌다.
`misfire_grace_time`이 10초라 맥이 잠들었다 깨면 이 경고가 뜬다.

**시각은 한국 시간으로 못박았다.** 기본값은 기기의 지역 시간인데, 지금은 맥이 한국 시간이라
그대로도 맞지만 다른 환경에서 돌리면 로그 시각과 DB의 `time_slot`이 어긋난다.
이 프로젝트는 시간대 불일치로 이미 여러 번 헤맸다.

**쓰는 법** — 파일 맨 위에서 logger를 하나 만들고 `print` 대신 부른다.

```python
logger = logging.getLogger(__name__)
logger.warning("브리핑 생성 실패 - %s: %s", type(error).__name__, error)
```

f-string 대신 `%s`로 넘긴다. 그 레벨이 꺼져 있으면 문자열을 만드는 비용조차 들지 않는다.

남은 것: API 서버가 계속 꺼져 있어 데이터를 아예 못 가져오는 경우는 여전히 슬롯이 빈다.
일시적 실패는 재시도로 처리했지만(위 'API 호출 실패 처리' 참고) 지속적인 장애는 재시도로
해결되지 않는다. 지금은 ERROR 로그를 남기고 그 슬롯을 포기한다(다른 슬롯 예약은 살아 있다).

---

## 겪은 문제와 해결

같은 실수를 반복하지 않기 위해 남긴다. 날짜는 발견한 날이다.

| 문제 | 원인 | 해결 |
|---|---|---|
| 모델 파일 import 자체가 실패 | `__table_args__`에 `UniqueConstraint`를 튜플로 감싸지 않음 | 뒤에 콤마를 붙여 튜플로 만듦. 제약이 하나여도 필요하다 |
| `DATABASE_URL` 없으면 앱 기동 불가 | `database.py`가 import 시점에 엔진 생성 | `get_engine()` 함수로 감싸 처음 쓸 때 생성 |
| SQL 로그가 터미널을 뒤덮음 | `echo=True` | 껐음. 쿼리를 볼 필요가 있을 때만 켠다 |
| SQL로 슬롯을 지우면 FK 제약에 막힘 | `cascade`는 ORM으로 지울 때만 동작 | 모델·DB 양쪽에 `ON DELETE CASCADE` 추가 |
| 07:30 지표가 조용히 비어 저장됨 | 지표 캐시가 서버 기동 시에만 채워짐. 스크립트로 실행하면 빈 상태 | 캐시가 비면 `_indicator_rows()`가 먼저 채운다 |
| 재수집 시 기존 브리핑 유실 | 덮어쓰기가 새 값의 성공 여부를 안 봄 | 새 값이 없으면 기존 값 유지 |
| 슬롯끼리 뉴스가 10건 중 9건 겹침 | 모든 슬롯이 "최근 24시간" 창을 씀 | 직전 슬롯 이후 구간으로 나눔 |
| 07:30의 중요한 밤 기사가 잘려나감 | 최신순으로 자름 | 중요도 점수순으로 고르고 표시 순서만 최신순 |
| 불개미 해설의 용어 정의가 틀림 | LLM이 설명을 창작(`매도세`를 반대로 씀) | 용어 19개를 우리가 정해뒀다. 처음엔 프롬프트에 넣었고, 지금은 `glossary.py`로 옮겨 프런트 호버용으로 쓴다(아래 항목 참고) |
| LLM 브리핑이 뒤쪽 슬롯부터 통째로 빔 | 제미나이 무료 한도 하루 20회 초과 | 모델을 Lite(하루 200회)로 교체 |
| 제미나이 타임아웃에 슬롯 생성이 통째로 죽음 | `httpx` 예외를 안 잡고 있었음 | `llm_client`에 재시도 추가, `httpx.HTTPError`까지 방어 |
| 슬롯 생성 속도를 10초로 잘못 판단 | 짧은 프롬프트로만 측정함. 실제 프롬프트는 뉴스 30건이 들어가 훨씬 길다 | 실측 78초로 정정 |
| `llm_client`에 재시도를 넣었다고 착각 | 상수와 주석만 들어가고 함수 본문이 안 바뀜. 다른 곳의 예외 처리가 넓어져서 증상이 가려졌다 | 전체 점검에서 발견해 실제로 적용. **주석이 코드와 다를 수 있으니 동작으로 확인할 것** |
| 지수 시간별 조회에서 `99:99:99` 응답 | 체결 없는 자리를 더미 값으로 채워서 보냄(분봉의 `88:88`과 같은 현상) | 시(hour) 자리가 00~23이 아닌 행을 걸러냄 |
| 응답의 `code` 필드가 DB 경로에서 항상 빈 값 | 업종코드·지표코드를 테이블에 저장하지 않음 | 스키마에서 제거. 항상 비는 필드는 두지 않는다 |
| 슬롯 3개가 저장 직전에 전부 실패(NOT NULL 위반) | `created_at`을 파이썬에서 넣도록 바꾸면서 DB 기본값을 지웠는데, **돌고 있던 서버는 옛날 코드**라 값을 안 보냈다. `fastapi run`은 자동 재시작이 없어서 코드를 고쳐도 서버는 예전 상태였다 | DB에도 `DEFAULT (now() AT TIME ZONE 'Asia/Seoul')`을 안전망으로 걸었다. **스키마를 바꾸면 서버를 반드시 재시작할 것** |
| 애프터마켓 데이터가 없다고 잘못 보고 | KRX 시간외단일가(거래 거의 없음)만 보고 NXT 애프터마켓(활발함)을 확인 안 함. `NX` 코드는 문서에 있었는데 정규장 중에만 테스트했다 | 시장구분 파라미터는 **그 값이 유효한 시간대에** 다시 호출해볼 것 |
| 급상승 TOP3가 브리핑에 반영되지 않음 | `briefing_service._build_data`가 지표·주도 섹터·뉴스만 받았다. 08:30 / 17:30 / 20:00은 주도 섹터도 지표도 비어서 **확정 수치가 통째로 없는 상태로 뉴스만 보고 브리핑을 쓰고 있었다** | `top_gainers`를 입력에 추가. 빈 항목은 아예 넘기지 않게 해서 LLM이 "데이터 없음"에 끌려가지 않게 했다 |
| 장 마감 슬롯을 15:35로 미뤘는데 효과가 없었음 | 장중 변화가 지표 **캐시**를 읽었다. 캐시는 10분 주기라 15:35에는 15:30 값이 들어 있었다. 5분을 기다린 의미가 사라진 상태 | `force_refresh=True`로 새로 받게 했다. 그 뒤 수집 시각도 15:30 정시로 통일했다 |
| KIS 장애 시 서버가 아예 안 뜸 | `main.py`가 `RuntimeError`만 잡음. 타임아웃은 `httpx` 예외라 lifespan 밖으로 나가 기동 실패 | `except Exception`으로 넓혔다. 지표 바만 끄고 앱은 띄운다 |
| 브라우저에서 수동 수집 불가 | CORS `allow_methods=["GET"]`인데 `POST /timeline/collect`가 있다. curl은 CORS를 안 봐서 안 드러났다 | `POST` 추가 |
| 주린이 해설이 용어 사전처럼 읽힘 | 프롬프트가 용어마다 괄호 설명을 붙이게 지시 | 괄호 설명을 금지하고 문맥 설명으로 전환. 용어집은 `glossary.py`로 옮겨 프런트 호버용으로 넘김 |
| `prompts.py`가 옛 버전으로 되돌아가 있었음 | 편집기에 열려 있던 옛 버전이 저장되며 덮인 것으로 보임. `GLOSSARY_TEXT`가 지워졌는데 `briefing_service`는 계속 참조해서 **`AttributeError`로 슬롯 전체가 저장 실패하는 상태**였다. 그 예외는 `except` 목록에 없어서 위로 그대로 올라간다 | `git checkout HEAD --`로 복구 후 다시 작업. **작업 시작 전에 `git status`를 먼저 볼 것** |
| 틀린 주석 다수 | 기능이 바뀌었는데 주석을 안 고쳤다. "아직 DB에 저장하지 않는다", "정시(00분/30분)마다", "published_at은 DB에 저장하지 않는다" 등 | 전부 수정. **주석이 코드와 다를 수 있으니 동작으로 확인할 것**(같은 교훈 두 번째다) |
| 슬롯 시각 정각 기사가 두 슬롯에 중복 | 뉴스 구간을 `시작 <= 발행 <= 끝` 양쪽 포함으로 잡았다. 08:30:00 기사가 08:30 슬롯과 09:30 슬롯에 둘 다 들어간다 | 끝을 배타적으로(`< 끝`) 바꿨다. `[20:00, 07:30)` `[07:30, 08:30)`처럼 빈틈도 겹침도 없다 |
| 15:30 브리핑이 장중 변화를 못 봄 | 장중 변화를 수집·브리핑이 **끝난 뒤에** 계산했다. "장 마감" 브리핑이 그날 시장이 얼마나 움직였는지를 모르고 뉴스·섹터만 보고 글을 썼다 | 순서를 바꿔 브리핑보다 먼저 만든다 |
| 급상승 TOP3 순서가 등락률 순이 아님 | NX 응답의 `data_rank`가 `prdy_ctrt`와 다른 기준으로 매겨져 있다(1위 영풍 +26.40% / 2위 에스투더블유 +29.96%). 당시에는 "거래소(J)는 정상"으로 봤는데, 상위 30개가 전부 상한가 근처라 우연히 맞아 보인 것이었다 | 30행을 받아 등락률로 직접 정렬했다. **근본 원인은 2026-09-14에 찾았다(아래 `FID_PRC_CLS_CODE` 항목)** |
| 08:30 슬롯 이름과 데이터 출처가 어긋남 | 슬롯 이름은 "NXT 프리마켓"인데 KRX 장전 예상체결을 썼다. 게다가 KRX 동시호가는 08:30에 시작해서 정각 조회 시 비어 있을 수 있다 | 세 슬롯 모두 NXT로 통일. 예상체결 함수는 대체 수단으로 남겨뒀다 |
| 07:30 지표가 10분 낡을 수 있었음 | 캐시를 읽는데 지표 바 갱신이 07:30 정각에 같이 예약돼 있어 순서를 보장할 수 없다. 지수 5종은 시장이 닫혀 같은 값이지만 **환율은 24시간 움직인다** | 이 슬롯도 `force_refresh=True`. 15:30 장중 변화의 기준점이고 놓치면 되살릴 수 없다 |
| 서버를 닫으면 실패 원인이 사라짐 | 전부 `print`였다. 슬롯 3개가 날아간 원인을 찾는 데 한참 걸렸다 | `logging`으로 전환(16곳) + 파일 보관. 레벨·스택·KST 시각이 남는다 |
| 주린이 해설이 스펙과 다른 재료를 씀 | 스펙은 "원본 데이터 + 브리핑"인데 브리핑만 넘겼다. 스펙 검토(7건 보고)에서도 놓쳤다 | 해설에 [데이터]를 추가. 슬롯별 재료 표를 만들어 스펙과 한 줄씩 대조했다 |
| 전체 점검을 여러 번 했는데 틀린 주석이 계속 남음 | 동작을 한 파일에서 바꾸고 **다른 파일의 설명은 안 찾았다**. 08:30을 NXT로 바꾼 뒤에도 `kis_client`·모델·스키마에 "08:30은 예상체결", `_indicator_rows`에 "15:35에 수집", `get_index_category_price`에 "코스닥은 파생지수만 온다"가 남아 있었다. 점검도 버그·실행 위주로 해서 주석 정합성은 체계적으로 보지 않았다 | 주석을 경위 없이 역할·사용법만 남기도록 전면 재작성(1,050줄 -> 433줄). 코드는 AST 비교로 한 글자도 안 바뀐 것을 확인. 이력은 이 문서로 옮김. **동작을 바꾸면 전 파일에서 관련 키워드를 grep할 것** |
| 07:30 코스피·코스닥 등락률이 0.00%로 저장됨 (2026-09-14 첫 실데이터) | KIS 국내 지수 현재가 API가 개장 전에는 가격은 직전 거래일 종가로 두고 **등락률만 0으로 초기화**한다. 주말 테스트에서는 드러나지 않았다. 브리핑도 코스피 하락(-1.76%)을 모른 채 "미국 증시 반등"만 썼다 | 장 밖에서 0이 오면 일자별 지수의 직전 거래일 등락률로 교체(`market_indicator_service._closed_domestic_change_rate`). 9/14 07:30 DB 값 두 칸(-1.76 / -1.95)을 수동 수정하고, 저장된 지표·뉴스로 브리핑·해설만 다시 생성해 덮어씀(뉴스·지표 행은 그대로). **개장 전 시간대 값은 개장 전에 실호출로 확인할 것** |
| 주도 섹터 "상승 1위"가 틀림, 급상승 TOP3가 진짜 상위를 놓침 (2026-09-14 09:30 실데이터) | 등락률 순위 API의 `FID_PRC_CLS_CODE`를 `"0"`으로 보냈다. 이 값은 **당일 저가 대비 상승률** 순이다. 09:30 종이·목재에서 페이퍼코리아(-1.12%)가 "상승 1위"로 저장됐는데 실제 1위는 한솔홈데코(+1.72%). 섬유·의류도 제이에스코퍼레이션(+1.93%)이 뽑혔지만 일신방직(+7.65%)이 1위. NX 30행을 재정렬해도 저가 대비로 잘린 30행 안에서만 고르므로 **08:30 급상승 TOP3에서 영풍 같은 진짜 상위가 빠졌을 수 있다** | `"1"`(전일 종가 대비)로 변경 → 섹터 4개·J 전체·NX 전체 모두 `prdy_ctrt` 내림차순 확인. 상승 1위는 `prdy_ctrt` 최댓값으로 고르게 방어 코드 추가. 9/14 08:30·09:30 저장값은 그 시점 가격을 되살릴 수 없어 그대로 둠. **파라미터 기본값 "0"의 뜻을 문서로 확인하지 않고 썼다 — 순위 API는 응답 순서가 화면 숫자 순인지 반드시 검증할 것** |
| 9/14 첫날 저장 데이터 사후 검증에서 LLM 문장 오류 다수 | 브리핑·해설이 (1) 틀린 입력(08:30 TOP3)을 그대로 씀 (2) 뉴스에 없는 원인을 지어냄("금리 불확실성 때문에 코스피 하락") (3) 데이터를 뉴스 출처로 적음 (4) 시제 오류(16:00 개장 예정을 "개장했습니다") (5) 급락장 섹터 TOP3(가장 덜 빠진 업종)를 "주요 업종 약세"로 씀 (6) % 누락 | 9/14 07:30·08:30·09:30을 뉴스 요약·API 값과 한 문장씩 대조해 틀린 문장 9개를 직접 수정, 08:30은 올바른 TOP3로 재생성 후 수정. 같은 날 11시에 프롬프트를 고쳤다: `BRIEFING_PROMPT` 데이터 설명에 주도 섹터(모두 마이너스면 가장 적게 내린 업종)·급상승 종목(이유 없음) 의미 추가, 규칙 12~14(원인은 같은 기사에 있을 때만 / "따르면"은 뉴스에만 / 예정된 일은 예정으로), `BEGINNER_PROMPT` 규칙 10~13 같은 취지, `SLOT_FOCUS` 08:30·17:30의 "왜 올랐는지"를 "뉴스에 나온 경우에만 연결"로, `REPORT_PROMPT` 규칙 10에 같은 금지 추가. `briefing_service._build_data`는 가격을 정수로(`46700.0원`·`12230유로` 같은 이상 출력이 나와서), 등락률 키에 `(%)`. 저장 데이터로 재생성(저장 안 함) 결과 출처·시제·이유 없음 표기가 정상. **LLM이라 0건을 보장할 수는 없어 슬롯마다 저장 후 대조 검증을 계속한다.** **저장 여부뿐 아니라 저장값·문장이 맞는지까지 대조할 것** |
| 로그가 apscheduler 잡음에 묻힘 | 작업 등록마다 두 줄씩 INFO를 남긴다. 작업이 9개라 기동 로그 23줄 중 19줄이 그것이었다 | `apscheduler`/`httpx`를 WARNING으로 낮췄다(23줄 -> 3줄). 경고·오류는 남겨둔다 |

반복해서 물린 것 두 가지는 따로 적어둔다.

1. **주석이 코드와 다를 수 있다.** `llm_client`에 재시도를 넣었다고 착각한 일(상수와 주석만 들어가고
   함수 본문이 안 바뀜), 기능이 바뀐 뒤 주석을 안 고친 일이 여러 번 있었다. **동작으로 확인할 것.**
2. **스키마나 코드를 바꾸면 서버를 재시작해야 한다.** `fastapi run`은 자동 재시작이 없다.
   돌고 있는 서버가 옛 코드라서 슬롯 3개가 통째로 실패한 일이 있었다.

## 팀 협업 규칙 (2026-09-10 기준)

1. `core` 폴더는 전역으로 모든 작업자가 사용
2. API 호출 파일은 `(api명)_client.py` 형식으로 작성 (예: `kis_client.py`)
3. 같은 API라도 함수마다 가져오는 데이터가 다르므로 각 함수 위에 주석으로 명시
4. **`core`를 수정하더라도 main에 바로 올리지 않고 본인 브랜치에서 작업한다. 최종적으로 브랜치를 합칠 때 함께 병합한다.**
   - 초기에는 "core만 main에 먼저 push하고 각자 받아가는" 방식이었으나, 브랜치별로 작업하다 마지막에 합치는 방식으로 변경됨
   - 대신 병합 시점에 `config.py`처럼 여러 명이 같이 건드리는 파일에서 충돌이 날 수 있으니, 합치기 전에 서로 어떤 파일을 건드렸는지 공유할 것
5. 병합 전에는 서버를 띄워 동작 확인 (`uv run fastapi dev main.py`)
6. `.env`는 git에 올라가지 않으므로 수정 시 각 담당자에게 재전달

**병합 시 공유할 것 (timeline 브랜치가 건드린 `core`)**

| 파일 | 내용 |
|---|---|
| `core/logging_config.py` | **새로 추가.** 로그 설정. 각 파일에서 `logger = logging.getLogger(__name__)`로 쓰면 된다. `setup_logging()`은 `main.py` lifespan에서 이미 부른다 |
| `core/database.py` | **사용법이 바뀜.** `engine` 직접 참조 -> `get_engine()` / `get_session_factory()` |
| `core/kis_client.py` | 함수 추가(업종·종목순위·휴장일·예상체결·투자자 매매동향·업종 일/주별·해외 기간별), 재시도·토큰 캐시 |
| `core/naver_client.py` | 새로 추가. 네이버 뉴스 검색 |
| `core/llm_client.py` | 새로 추가. 제미나이 (모델 `gemini-3.5-flash-lite`) |
| `core/image_client.py` | 새로 추가. Cloudflare Workers AI 이미지 생성 (FLUX.1 schnell) |
| `core/storage_client.py` | 새로 추가. Supabase Storage 업로드 → 공개 URL |
| `core/config.py` | 키 필드 추가(Cloudflare·Supabase Storage 포함). **여러 명이 같이 건드리는 파일이라 충돌 주의** |

Supabase 테이블 16개(타임라인 9 + 보고서 7)와 Storage 공개 버킷 `report-images`도 이 브랜치에서 만들었다. `created_at`은 전 테이블 한국 시간이다.
`.env`에 추가된 키: `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`(`.env.example`에 발급 위치 적음).

---

## 보고서(브리핑 탭) 설계 — 확정 (2026-09-13)

> 아래 스펙과 데이터 출처는 전부 실제 호출로 확인했고, 2026-09-13에 1~6단계 코드로 구현했다.
> 구현 결과와 확인할 것은 맨 아래 '다음 작업 > 보고서 구현 진행 상황'을 본다.

### 원칙

1. **타임라인 수집 구조(07:30~20:00)는 바꾸지 않는다.**
2. **20:00 슬롯이 끝나면 이어서 바로** 보고서용 수치를 조회·저장하고 LLM이 보고서를 쓴다.
3. **LLM 문구는 반드시 수집한 데이터를 기반으로 쓴다 — 타임라인 브리핑과 같은 로직.**
   먼저 데이터를 모아 저장하고, 그 데이터를 `확정 수치`(API 값)와 `뉴스`로 나눠 넘기며, 없는 수치·종목을 만들지 못하게 한다.
   카드·차트의 숫자는 LLM이 쓰지 않고 코드가 채운다.
4. 일간 = 그날 하루 기준. 주간 = 그 주 거래일 기준(거래일이 적은 주는 있는 날만). 주간은 그 주 일간 보고서에 저장된 값을 모아 만든다.

### 필요한 데이터와 출처

| 섹션 | 항목 | 일간 | 주간 |
|---|---|---|---|
| 메인 | 타입 | `DAILY` | `WEEKLY` |
| 메인 | 발행 시각(KST) | 코드 | 코드 |
| 메인 | 제목 · 한 줄 요약 · 섹션 타이틀 3개 | LLM (그날 슬롯 데이터 + 아래 수치) | LLM (그 주 일간 보고서) |
| 메인 | 메인 이미지 | **한 줄 요약** → 이미지 생성 | 같음 |
| 섹션1 | 타이틀 · 설명 · 핵심 요약 3개 | LLM (뉴스·브리핑·해설) | LLM |
| 섹션1 | 섹션 이미지 | **섹션1 설명** → 이미지 생성 (9/14 사용자 결정, 전에는 타이틀·설명) | 같음 |
| 섹션2 | 타이틀 · 설명 · 핵심 요약 3개 | LLM | LLM |
| 섹션2 | 외국인 순매도 | 당일 값 | 그 주 거래일 합계 (KIS 일별 기록에서 직접 합산, 9/14 변경) |
| 섹션2 | 외국인 뱃지 `foreign_badge` (코드 계산, 9/14 추가) | "4거래일 연속 순매도" / "순매도 전환" / 같은 방향으로 더 컸던 날이 20거래일 이상 전이면 "8/25 이후 최대 순매도" | 주 합계로 같은 규칙: "3주 연속 순매도" / "5주 만에 최대 주간 순매도"(4주 기준) |
| 섹션2 | VKOSPI | 당일 종가 · 전일 대비 | 주 마지막 거래일 종가 · 전주 대비 |
| 섹션2 | VKOSPI 뱃지 `vkospi_badge` (응답에서 계산, DB 칼럼 없음) | "+18.2% 급등": 반올림한 등락률 ±10% 이상 급등·급락, 그 안 상승·하락, 0 보합 | 같음 |
| 섹션2 | 차트 분기 데이터 | 현 분기 포함 **6개** · 분기별 환율 · 외국인 순매도 | 같음 (현 분기 = 그 주까지) |
| 섹션3 | 타이틀 · 설명 · 핵심 요약 3개 | LLM | LLM |
| 섹션3 | 주목할 섹터 카드 **1개** (9/14 정정) | 코스피 21개 업종 중 **그날 종가 기준 등락률 1위** (15:30 슬롯에 의존하지 않음) | 코스피 21개 업종 중 **그 주 등락률 1위** |
| 결론 | 한 줄 요약 · 키워드 3개(타이틀·설명) | LLM | LLM |
| 결론 | 어려운 용어 3개(용어명·설명) | 사전 우선 (아래) | 같음 |

**API별 확인 결과**

| 데이터 | API | 확인한 것 |
|---|---|---|
| 외국인·기관·개인 순매수 | `시장별 투자자매매동향(일별)` `FHPTJ04040000` | 단위 **백만원**. 9/11 -2,298,412 = -2조 2,984억(보도 2조 3,040억). 300거래일씩, 기준일을 옮기면 이전 구간. 날짜 지정 조회 → **나중에도 복구 가능** |
| VKOSPI 일간 | 업종 `0503` (`get_domestic_index_price` / `국내업종 일자별지수` `FHPUP02120000` D) | 9/11 46.28, -2.12% |
| VKOSPI 주간 | 같은 API `W` | 9/7주 46.28, 전주 대비 +17.67% |
| 차트 외국인 분기 | 위 투자자 API 3회 호출 → 분기 합산 | 25/Q1~26/Q3. 월별 달러 환산이 보도와 일치(7월 69억달러 vs 66억, 8월 74억 vs 72억) |
| 차트 환율 분기 | `해외 기간별시세` `FHKST03030100` X / `FX@KRW` M | 월별 33개월(2024-01~). 분기 마지막 달 값 사용 |
| 섹터 카드 (일간) | `국내업종 일자별지수` output1 | 등락률, 상승·하락·보합 종목 수, 거래대금, 전일 거래대금 |
| 섹터 카드 (주간) | 같은 API `W` output2 | 주간 등락률, 주간 거래대금(9/7주 건설 +11.03%, 3조 2,927억, 전주 대비 +28%). **상승 종목 수는 주간 값이 없음** |

**차트 분기 창** — 보고서 날짜로 "현 분기 + 직전 5개"를 계산한다. 코드에 분기를 적지 않으므로 자동으로 밀린다.

```
2026-09-11  25/Q2 25/Q3 25/Q4 26/Q1 26/Q2 [26/Q3]
2026-10-01        25/Q3 25/Q4 26/Q1 26/Q2  26/Q3 [26/Q4]
2027-01-04              25/Q4 26/Q1 26/Q2  26/Q3  26/Q4 [27/Q1]
```

과거 분기는 배경 데이터라 **이 부분만 보고서 생성 때 API로 조회**한다(타임라인 전제의 유일한 예외).

**섹터 카드** — 종목 카드는 쓰지 않는다(급상승 종목은 증권사 커버리지가 없어 카드가 빈다).
~~LLM이 섹션1 이슈와 가장 관련된 업종을 후보 중에서 고르고~~ → **9/14 정정: 사용자 스펙은 처음부터 업종 하나("주목할 섹터의 데이터")였는데 3개·LLM 선택으로 잘못 옮겨 구현했다.** 일간은 15:30 주도 섹터 1위, 주간은 주간 등락률 1위를 코드가 고른다. 박스 3개 = 그 업종의 등락률 / 상승 종목 비율 / 거래대금. 비율은 기준을 같이 표기한다.

```
┌ 업종 등락률 ┐ ┌ 상승 종목 비율          ┐ ┌ 거래대금 (전일 대비)     ┐
│ +1.90%     │ │ 74%                    │ │ +30%                   │
│ 건설       │ │ 업종 35개 중 26개 상승   │ │ 6,075억 (전일 4,669억)  │
```

- 저장된 업종명 → 코드: `KOSPI_SECTOR_CODES`와 API 업종명 21개 전부 일치(확인함). 이름이 바뀌면 경고 로그 + 그 카드만 비움
- **주의: 일자별지수 output1은 요청 날짜를 무시하고 항상 최근 거래일 값**을 준다(9/9·9/4를 요청해도 9/11 값).
  상승 종목 수·전일 거래대금은 **당일에 받아야 하고, 실패하면 다음 날 09:00 개장 전까지만 복구 가능**
- 주간 카드의 "상승 종목 비율"은 업종 지수에 주간 값이 없어 **1위 업종의 종목별 주봉으로 직접 센다**(`report_data_service._weekly_rising_counts`, 9/14)

**용어 3개** — `glossary.py`에 있는 용어 중 보고서 본문에 나온 것을 중요도 순(결론·핵심 요약에 나온 것 우선)으로 3개.
모자라면 본문의 다른 용어로 채우고, 그 설명(LLM 작성)은 따로 기록해 검토 후 사전에 정식 등록한다.
**사전 확장이 필요하다**(9/10~11 콘텐츠에서 사전 19개 중 13개만 등장, 사전 밖 용어 41개 등장).

### 이미지 (메인 · 섹션1, 보고서당 2장)

- **Cloudflare Workers AI `@cf/black-forest-labs/flux-1-schnell`** — 무료 하루 10,000 뉴런(장당 약 58뉴런 추정). 생성 1.5~4.7초
- 키: `.env`의 `CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_API_TOKEN` (`config.py` 필드 추가 완료)
- 요청: `POST https://api.cloudflare.com/client/v4/accounts/{ACCOUNT_ID}/ai/run/@cf/black-forest-labs/flux-1-schnell`, body `{"prompt", "steps": 4}` → `result.image`(base64 JPEG)
- **정사각형 1024×1024만** 된다(`width`/`height`는 400). 프런트가 가운데를 잘라 쓰므로 주요 사물은 가운데
- **한국어 문장을 그대로 넣으면 무관한 그림**이 나온다 → LLM이 영어 장면 묘사로 바꾸는 단계 필수
- **글자 금지 유지**(이미지 AI가 한글을 못 씀). 지폐·칩처럼 원래 글자가 있는 사물은 깨진 글자가 섞일 수 있다
- 스타일(2026-09-14 사이트 시안 기준으로 교체): 밝은 회색 바탕·빨간 포인트·차콜·카드형 그림자의 **플랫 일러스트**(시안의 실사 사진은 더미라 무시하기로 사용자 결정)
  - 시험에서 확인한 것: "horizontal band / panoramic"을 넣으면 위아래에 **남색 레터박스 띠**를 그린다 → 제거, "full-bleed, no borders/bars"
  - 증서·서류는 **깨진 글자**("GOVERNMT BCND")가 들어간다 → 글자 있는 사물 금지
  - **화살표·그래프 방향을 못 지킨다**: 아래 화살표 3번 중 1번만 아래, "떨어지는 선"은 올라가게 그림 → 방향 사물 금지, 분위기는 조명으로만
  - 화면에서는 가로 배너로 가운데를 잘라 쓰므로 사물을 가로로 나란히, 세로 가운데에 둔다
- 프롬프트 조정 과제: 예시 사물 목록을 넣으면 LLM이 끌려간다(급락 요약에 배가 나옴) → "요약에 나온 대상만". 상승·하락 분위기는 조명으로 구분
- 저장: **Supabase Storage에 올리고 URL만 DB에 저장**. `.env`의 `SUPABASE_URL` / `SUPABASE_SERVICE_KEY`, 공개 버킷 `report-images` (준비 완료)
- 기각: 제미나이 이미지(무료 한도 0), ChatGPT Plus(API 별도 과금), Pollinations(품질), 뉴스 사진 og:image(저작권), Hugging Face(월 $0.10)

### 생성 시점

- 일간: **20:00 슬롯 완료 직후** 이어서 실행 (휴장일에는 슬롯과 함께 건너뜀)
- 주간: **그 주의 마지막 거래일** 일간 보고서 직후 (추석 주처럼 금요일이 휴장이면 목요일) — 판정은 `is_trading_day`로

### 미정

1. ~~주간 섹터 카드의 상승 종목 비율 기준~~ → **9/14 해결: 종목별 주봉으로 계산**(그 주 종가 > 전주 종가인 종목 수 / 주봉이 있는 종목 수). 9/7주 건설 35개 중 20개 상승, 약 4초 (종목 명단은 9/14부터 종목 마스터 파일 — '9/14 확인할 것' 7번)
2. VIX 사용 여부 — 해외지수 API 심볼 `VIX`로 조회됨(9/11 15.84, -11.21%, 보도와 일치). 미국 종가라 한국 시간 다음 날 새벽 확정.
   **틀린 심볼도 `rt_cd=0`에 0.00을 주므로** 종목명·값 검증 필요

### 테이블 설계 (1단계 — 구현·Supabase 생성 완료, 2026-09-13)

`models/report.py` + `create_tables.py` import 추가. 테이블 7개 생성, 기존 타임라인 테이블·데이터는 그대로.
테스트 행으로 확인함: ORM 저장, `created_at` DB 기본값(SQL로 넣어도 채워짐), SQL로 부모 삭제 시 자식 6개 테이블 연쇄 삭제. 테스트 행은 지웠다.

**프런트 조회 기준 (타임라인과 같은 방식 — 날짜를 주면 그 데이터)**
- 일간: `report_type="DAILY"` and `start_date = 날짜`
- 주간: `report_type="WEEKLY"` and `end_date = 날짜` — **그날 만든 보고서만**(그 주 마지막 거래일에만 있다). 2026-09-14 사용자 결정: 프런트는 해당 일 데이터만 불러온다(처음엔 "그 날짜가 속한 주"였음)
- 새 테이블은 `created_at`에 `server_default`를 모델에 직접 걸었다(타임라인 테이블은 SQL로 따로 걸었음)

```
timeline_report ──┬── timeline_report_section        섹션 3행 (seq 1~3)
                  │       └── timeline_report_point  핵심 요약 3행씩
                  ├── timeline_report_quarter        차트 분기 6행
                  ├── timeline_report_sector         섹터 카드 3행
                  ├── timeline_report_keyword        결론 키워드 3행
                  └── timeline_report_term           어려운 용어 3행
```

| 테이블 | 칼럼 | 비고 |
|---|---|---|
| `timeline_report` | `report_type`(DAILY/WEEKLY), `start_date`, `end_date`, `title`, `summary`(한 줄 요약), `main_image_url`, `conclusion`(결론 한 줄 요약), `foreign_net_buy`, `institution_net_buy`, `individual_net_buy`(백만원), `vkospi`, `vkospi_change_rate`, `published_at`, `created_at` | 유니크 (`report_type`, `start_date`). 일간은 start=end=그날, 주간은 그 주 첫·마지막 거래일 |
| `timeline_report_section` | `report_id`, `seq`, `title`, `description`, `image_url`(섹션1만) | |
| `timeline_report_point` | `section_id`, `seq`, `body` | 핵심 요약 |
| `timeline_report_quarter` | `report_id`, `seq`, `label`("26/Q3"), `usd_krw`, `foreign_net_buy`(백만원), `is_current` | 현 분기는 보고서 날짜까지 |
| `timeline_report_sector` | `report_id`, `sector_name`, `change_rate`, `rising_count`, `total_count`, `trade_amount`, `prev_trade_amount` | 보고서당 1행. 주간 종목 수는 종목별 주봉으로 계산 |
| `timeline_report_keyword` | `report_id`, `seq`, `title`, `description` | |
| `timeline_report_term` | `report_id`, `seq`, `term`, `description`, `source`(GLOSSARY/LLM) | LLM 작성분은 검토 후 사전 등록. `source`는 백엔드 검토용이라 응답에서 뺌(9/14) |

- 일간·주간은 화면 구조가 같아서 **한 테이블 + `report_type`** 으로 둔다(조회 API·저장 코드 하나)
- 수치(투자자 순매수·VKOSPI·분기·섹터)를 먼저 저장하고 LLM 문구 칼럼은 나중에 채운다 → 문구 칼럼은 전부 NULL 허용(LLM 실패 시 슬롯과 같은 처리)
- 기관·개인 순매수는 카드에는 안 쓰지만 같은 API 응답에 있어 LLM 근거용으로 저장
- 금액은 `BigInteger`(분기 합산이 수십조 = 수천만 백만원). 섹터 거래대금도 백만원(2단계 실호출로 확정)
- 타임라인 규칙 그대로: FK `ondelete="CASCADE"` + ORM `cascade`, `created_at` 파이썬 기본값 + DB `DEFAULT`
- **재실행은 타임라인과 다르다.** 슬롯은 지우고 다시 넣지만, 보고서는 같은 행을 고쳐 쓴다(LLM이 실패해도 기존 문구가 남도록 — '3단계 결과')

---

## 실데이터 적재 일정

**2026-09-14(월) 07:30부터 실데이터를 쌓는다.**

**9/13(일) 밤 테스트 데이터를 전부 지웠다.** 타임라인 16개 테이블 0행, Storage `report-images` 비움.
지우기 전에 9/10 일간·9/7주 주간 테스트 보고서를 `backend/logs/report_sample_20260913.json`,
이미지 4장을 `backend/logs/report_sample_images/`에 백업했다(깃 제외 폴더, 9/14 사용자 확인용).

### 월요일 아침 체크리스트

**먼저 서버를 재시작해야 한다.** `fastapi run`은 자동 재시작이 없어서 주말에 바꾼 코드가
반영돼 있지 않다. `caffeinate`도 꼭 붙인다 — 없으면 맥이 잠들어 스케줄러가 멈춘다.

```
cd backend && caffeinate -dis uv run fastapi run main.py
```

확인할 것

1. **07:30 슬롯이 저장됐는지.** 실패하면 그 시점 환율을 되살릴 수 없고 15:30 장중 변화가 빈다.
   비었으면 바로 `POST /timeline/collect/0730`
2. **08:30에 NXT 프리마켓 값이 채워지는지.** 비면 `top_gainer_service.SOURCE_BY_SLOT`의
   `"0830"`을 `"expected"`로 되돌리면 KRX 예상체결로 돌아간다(함수는 남겨뒀다)
3. ~~15:30 `closing_price`가 실제 종가와 같은지~~ → **9/14 달랐음(15:29 값). 15:35로 변경. 9/15에 종가·업종 순위 일치 재확인**
4. **17:30 급상승 종목 값.** 9/14가 한국거래소 애프터마켓 개설일이라는 보도가 있어서(미확인)
   NXT만 보는 지금 방식이 달라질 수 있다
5. **제미나이 한도 200회가 맞는지.** `브리핑 생성 실패` 로그가 몇 번째 슬롯부터 나오는지 본다
6. **뉴스 구간별 기사 수.** 08:30 / 09:30은 구간이 1시간이라 부족할 수 있다
7. **20:00 슬롯 뒤 일간 보고서.** `[스케줄러] 일간 보고서 … 저장 완료` 로그와 `GET /timeline/report?type=daily`.
   실패하면 20:00~다음 날 09:00 사이에 `POST /timeline/report/daily`로 다시 만든다(상승 종목 수가 그때까지만 받아진다)
8. **(9/15 추가) 07:30 코스피·코스닥 등락률이 0.00%가 아닌지.** 9/14 수정 코드가 반영된 서버여야 한다

로그로 한 번에 확인한다.

```
grep -E "WARNING|ERROR" logs/timeline.log
```

---

## 다음 작업

> **작업 인계 규칙**: 토큰이 떨어지면 다른 도구(Codex 등)로 이어서 작업한다. 그래서 단계가 끝날 때마다
> 아래 진행 상황을 갱신한다. 이어받는 쪽은 이 문서 맨 위 '코드 주석 방침', 위 '보고서(브리핑 탭) 설계 — 확정',
> 아래 진행 상황 순서로 읽는다. 커밋·푸시는 사용자가 지시할 때만. KIS 토큰은 반드시 캐시(`kis_client.get_access_token`)를 거친다
> (강사님 계좌라 재발급마다 알림이 간다). `.env`는 커밋하지 않는다.

**보고서 구현 진행 상황**

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 모델(테이블) `models/report.py` + `create_tables.py` | **완료** (Supabase 생성·저장·연쇄 삭제 확인) |
| 2 | core API — `kis_client`(투자자 매매동향·업종 일/주별·환율 기간), Cloudflare 이미지, Supabase Storage, `config.py` 키 | **완료** (아래 '2단계 결과') |
| 3 | 리포지토리 — 보고서 저장·조회 `services/report_repository.py` | **완료** (아래 '3단계 결과') |
| 4-1 | 서비스 — 일간 수치 수집·저장 `services/report_data_service.py` | **구현 완료** |
| 4-2 | 서비스 — 프롬프트(`prompts.REPORT_PROMPT`·`IMAGE_STYLE`), 문구·섹터·용어·이미지, 주간 `services/report_service.py` | **구현 완료** |
| 5 | 스케줄러 연결 — `timeline_service.run_scheduled_collect("2000")` 끝에서 `report_service.run_scheduled_reports()` | **구현 완료** (실제 20:00 동작은 9/14 확인) |
| 6 | 스키마 `schemas/report.py` · 라우터 `GET /timeline/report`, `POST /timeline/report/daily`·`/weekly` | **구현 완료** |
| 7 | **사용자 코드·결과 확인 (9/14 예정)** → 품질 조정 | **다음** |

> 2026-09-13 밤: 전체 검토 후 사용자 지시로 커밋했다(타임라인 주석 정리 + 보고서 1~6단계). 코드·결과 확인은 9/14에 사용자가 한다.

**4-2 · 5 · 6단계 결과 (2026-09-13 구현, 9/10 테스트 데이터로 끝까지 실행 확인)**

생성 흐름 (`report_service`)
1. `generate_daily(session, day)`: `report_data_service.collect_and_save_daily`(수치 저장) → `timeline_repository.load_day`(그날 슬롯)
   → `_daily_input`(재료 JSON: `확정 수치`=투자자·VKOSPI·분기·섹터 후보·시간대별 시세 / `타임라인`=슬롯 브리핑 / `뉴스`=슬롯 뉴스, URL 중복 제거) → `_write_report`
2. `generate_weekly(session, day)`: `_week_trading_days`(월~금 중 `is_trading_day`) → 그 주 일간 보고서 로드(없으면 None)
   → `collect_weekly_data`(투자자=일간 합, VKOSPI=W, 분기=주 마지막 거래일 기준 재계산) + `collect_weekly_sector_cards`(후보=그 주 일간 섹터 카드 업종명 전체, W 값)
   → 수치 저장 → `_weekly_input`(`확정 수치` + `일간 보고서` 문구) → `_write_report`
3. `_write_report`: LLM 1회(`REPORT_PROMPT`, timeout 120초) → `_parse_content`(섹션 3개·제목·요약·결론 없으면 실패 처리)
   → 섹터 카드 선택(`_pick_sector_cards`: LLM 순서 + 남은 후보로 채움, 최대 3) 저장 → 문구 저장(`_pick_terms`) → 이미지 2장 병렬 생성·업로드 → URL 저장.
   LLM 실패 시 기존 문구·섹터 카드를 건드리지 않고 반환(섹터 카드가 아직 없을 때만 후보 앞 3개 저장 — 주간 첫 생성)
- **LLM 호출은 보고서당 1회.** 이미지용 영어 장면 묘사(`main_image_scene`, `section1_image_scene`)도 같은 응답에서 받는다(제미나이 한도 절약). 코드가 뒤에 `prompts.IMAGE_STYLE`을 붙인다
- 금액은 LLM 입력에서 `_won()`으로 "-2조 2,984억원"처럼 바꿔 넘긴다. 등락률 키에는 "(%)"를 붙였다(처음 실행에서 VKOSPI "-3.96"을 "3.96 하락"으로 써서 추가)
- 용어 선택(`_pick_terms`): 사전 용어 중 본문에 나온 것(결론·핵심 요약·키워드 우선) → 모자라면 LLM 후보 중 본문에 실제로 있는 것(`source="LLM"`)
- 이미지 경로: `{daily|weekly}/{start_date}/{main|section1}_{HHMMSS}.jpg` (CDN 캐시 회피)
- 스케줄러: 20:00 슬롯 수집이 **실패해도** 보고서는 시도한다. 휴장일이면 건너뛰고, 오늘이 그 주 마지막 거래일이면 일간 뒤 주간. 일간이 실패해도 주간은 시도. 로그 `[스케줄러] 일간 보고서 … 저장 완료 (N초) - 문구 있음/없음, 이미지 있음/없음`
- API: `GET /timeline/report?type=daily|weekly&date=YYYY-MM-DD` (없으면 `null`, 잘못된 type 404),
  `POST /timeline/report/daily?date=`, `POST /timeline/report/weekly?date=`(일간 보고서 없으면 404). 응답 금액은 백만원, `rising_ratio`·`trade_amount_change_rate`는 `to_response`가 계산

실행 결과 (9/10 일간 → 9/7주 주간, 테스트 데이터)
- 일간: 제목 "중동 불안 속 7000선 사수", 섹션 3개·핵심 요약 각 3개, 메인·섹션1 이미지 업로드, 외국인 -2조 6,217억, VKOSPI 47.28 -3.96%,
  섹터 의료·정밀기기 +2.25%(거래대금 +55.5%)·일반서비스·비금속. 상승 종목 수는 None(9/10이 최근 거래일이 아니라서 — 의도대로)
- 주간: 기간 9/7~9/11, VKOSPI 46.28 +17.67%, 섹터 주간 값(일반서비스 +6.68% 등), `GET ?type=weekly&date=2026-09-09` → 9/7주 조회됨(당시 기준. 9/14 조회 기준 변경 후에는 `date=2026-09-11`로만 조회된다)
- 용어는 3개 모두 사전(국채금리·순매도·순매수)

**9/14 실데이터 사후 수정 기록** — 08:30 급상승 TOP3를 NXT 분봉(`FHKST03010200`, `082900` 봉 = 08:30:00 시점 값, 저장 스냅샷과 일치 확인)으로
복원해 교체(영풍 +21.46% / 위메이드맥스 +18.98% / 일신방직 +18.39%, 후보 112종목 기준 — 전 종목 대상은 아님).
09:30 섹터 상승 1위를 J 분봉 `092900` 봉으로 업종 전 종목(섬유·의류 28/28, 종이·목재 19/19, 음식료·담배 47/46) 대조해 교체
(섬유·의류 일신방직 "상승·거래 1위", 종이·목재 한솔홈데코 +1.77%, 음식료·담배 사조동아원은 원래 맞음).
12:00 검증(업종 지수 틱 21/21, 업종 전 종목 J 분봉 `115900` 봉): 섹터 TOP3(운송·창고·제약·오락·문화)·상승 1위·거래 1위 모두 일치. 서버가 프롬프트 수정 뒤 재시작되지 않아 옛 프롬프트로 생성됨 → 제목 "혼조"(코스피 -2.27%인데)·해설 2개(뉴스에 없는 원인) 직접 수정. 검증 스크립트는 scratchpad `verify_sector.py HHMMSS`(세션 임시 폴더)로 돌렸다. 참고: 제약 상승 1위가 우선주 `유유제약2우B`(거래대금 447만원)라 대표성 검토 필요
14:00 검증(서버 13:53 재시작 후 새 프롬프트): 섹터 TOP3(운송·창고·오락·문화·제약)·상승 1위·거래 1위 틱·분봉과 일치. 해설 3개가 "거래가 몰리면서 업종이 올랐다"는 가짜 원인 + 브리핑 숫자 반복 → 뉴스 기준으로 직접 수정, `BEGINNER_PROMPT` 규칙 14(거래대금을 원인으로 쓰지 말 것)·15(브리핑 반복 대신 의미 설명·다른 뉴스) 추가, 재생성 시험에서 가짜 원인 사라짐 확인
섹션 스펙 보완(사용자 시안 기준): 섹션1 이미지는 **섹션1 description 기준**, 섹션 타이틀 35자(메인 프리뷰에도 쓰임), 섹션 설명 두 문단 350자
**KRX 애프터마켓이 9/14 16:00~20:00 실제로 열렸다(08:30 뉴스로 확인, 시간외 단일가 폐지)** → 17:30·20:00 급상승 종목을 NX만 볼지 UN으로 볼지 17:30 데이터로 판단
**17:30 검증(19:16, NXT·KRX·UN 1분봉 `173000` 봉, 후보 57종목 = 19:18 세 시장 순위 합집합)**: 저장 TOP3(영풍 +29.91% 49,950 / 위메이드맥스 +29.84% 5,200 / 일신방직 +17.42% 12,130) = NXT 봉과 일치. 뉴스 5건 구간 정상, 브리핑 사실은 기사와 일치.
- KRX 애프터마켓 실측: 일신방직 KRX 15:30 12,250 → 17:00 12,500 → 17:30 12,140(거래량 1,678주, 같은 분 NXT 31주). KRX(J) 17:30 상위는 인피니트헬스케어·광전자 +30.00%, KS인더스트리 +29.97% 등 **NXT에 없는 종목**(NX 봉 0). UN 순위 API는 0행, UN 분봉은 NXT 비상장 종목 가격이 틀림(인피니트헬스케어 8,700 vs 11,310) → **UN 사용 불가**
- 뉴스(9/13~14 다수): KRX 애프터마켓은 16:00~20:00 실시간 매매, 기존 시간외 단일가 폐지, 코스피·코스닥 2,766종목(ETF·ETN 제외). NXT 공통 거래 종목은 610개. 2월 보도는 "프리·애프터마켓 1년 시범 운영 뒤 2027년 말 24시간 거래", KRX 프리마켓은 연기(내년 추진)
- 문구 문제: 등락률이 전일 종가 대비라 정규장 상승분이 들어 있는데 "애프터마켓에서 영풍 29.91% 올랐다"고 씀(영풍은 정규장 상한가 마감 후 변동 없음) → 사용자 결정으로 **데이터·프롬프트 둘 다 수정**: DB 17:30 부제·브리핑 포인트 2·해설 2를 "전일 종가보다 OO% 높은 가격, 오른 폭은 정규장"으로 고침. `BRIEFING_PROMPT` 급상승 종목 설명, `BEGINNER_PROMPT` 규칙 16, `SLOT_FOCUS` 17:30·20:00에 "애프터마켓에서 올랐다고 쓰지 말 것", `briefing_service` 급상승 종목 키를 "전일 종가 대비 등락률(%)"로
- **결정(9/14 19:30, 사용자가 판단 위임)**: **17:30·20:00은 KRX(J), 08:30은 NXT 유지.** 원칙 = "그 시간대에 거래되는 시장 중 종목이 가장 넓은 곳".
  근거: KRX 애프터마켓 2,766종목(NXT 600여) · 저녁 거래량도 KRX가 큼(일신방직 1,678주 vs 31주) · 실제 애프터마켓 급등을 잡음(빛샘전자 15:30 11,240 → 17:30 +24.94% → 19시대 상한가, NXT 비상장) · 제도가 멈춰도 J 순위는 정규장 종가로 나와 빈 값이 저장되지 않음.
  감수: 08:30과 거래소가 다름(08:30은 슬롯 이름이 "NXT 프리마켓"이라 화면에 드러남). **KRX 프리마켓 개장(보도상 2027년 말) 때 "0830"도 "krx"로 옮길지 검토.**
  코드: `top_gainer_service.SOURCE_BY_SLOT` "1730"·"2000" → "krx"(`_MARKET_BY_SOURCE` NX/J), 등락률 같으면 누적 거래량(`acml_vol`) 큰 순(상한가 동률이 여럿), `SLOT_FOCUS` 17:30·20:00·`BRIEFING_PROMPT` 급상승 종목 설명을 시장별로, `kis_client.get_fluctuation_ranking` 주석(UN은 0행)
  실데이터: 9/14 17:30 TOP3를 KRX 1분봉 `173000`으로 교체 — 광전자 +30.00% 10,270 / 인피니트헬스케어 +30.00% 11,310 / KS인더스트리 +29.97% 1,904(셋 다 15:30 종가와 같음). 저장된 뉴스로 새 프롬프트 재생성 후 2곳 직접 수정(KS인더스트리 "거래를 마쳤습니다" 시제, 해설 2가 브리핑 숫자 반복 → 정규장 상한가 유지·전일 종가 기준 설명). `created_at` 17:30:06 유지(행을 지우지 않고 값만 수정). 후보는 19시대 J 순위 30개라 17:30에 상위였다가 그 뒤 빠진 종목은 확인 못 함

**9/14 보고서 프롬프트 보완(사용자 결정)** — `summary`는 **보고서 전체 요약**(메인), `conclusion`은 **결론 영역 "초보 개미도 30초만에 이해하는 한 줄 결론"의 한 줄 요약**으로
주식 입문자용 쉬운 풀이(전문 용어 금지, summary 반복 금지). 키워드 설명도 입문자 눈높이. 출력 순서를 섹션 → title·summary·conclusion·keywords로 바꿔
**섹션 3개를 먼저 쓰고 재료 전체 + 섹션을 바탕으로 요약·결론을 쓰게** 했다. "혼조" 오용 금지 규칙을 브리핑·보고서에 추가(12:00 제목, 보고서 시험 생성 섹션3에서 반복).
9/14 13시대 부분 데이터로 시험 생성(저장·이미지 없음): summary "미국 금리 인상 경계감과 중동 리스크로 외국인 매물이 쏟아지며 코스피가 하락했습니다." /
conclusion "미국 금리가 오를 것이라는 걱정과 중동 지역의 불안 때문에 외국인이 주식을 많이 팔아치우면서 주가지수가 큰 폭으로 내렸습니다."

**9/14 섹션2 뱃지 구현** — 9/13 스펙의 "VKOSPI 28.4pt, +18.2% 급등"에서 **단어("급등")를 빠뜨렸던 것**을 발견해 추가.
외국인 순매도 뱃지는 사용자와 정한 규칙(매일 값이 나오게 연속·전환 기본, 20거래일 이상 만의 최대면 대체). LLM 미사용.
- `models/report.py`에 `foreign_badge VARCHAR(50)` 추가, **Supabase에 `ALTER TABLE ... ADD COLUMN` 적용함**(create_tables는 기존 테이블 칼럼을 추가하지 않는다)
- `report_data_service._flow_badge(series, unit, max_gap, weekly)` + `_foreign_series`, 기준 상수 `_BADGE_MAX_DAYS=20` / `_BADGE_MAX_WEEKS=4`.
  같은 방향 기간이 기준보다 적으면 "최대"를 쓰지 않는다(막 전환한 날 "32거래일 중 최대 순매수"가 나와서 추가)
- `report_service._vkospi_badge` (`_VKOSPI_SURGE=10.0`, 반올림 후 판정 — 9.99가 "+10.0% 상승"으로 나와서 수정), `to_response`·`ReportResponse`에 `foreign_badge`·`vkospi_badge`, LLM 재료 확정 수치에 "외국인 순매수 흐름" 추가
- 실데이터: 9/14 "4거래일 연속 순매도", 9/11 "3거래일 연속 순매도", 9/7주 "4주 연속 순매도", 9/7주 VKOSPI "+17.7% 급등"
- 미사용 코드 점검(ruff F + 참조 없는 함수 검색): 미사용 import·변수 없음. 참조 없는 함수는 `kis_client.get_index_tick_price` 하나였다. 처음엔 슬롯 검증에 쓴다는 이유로 남겼으나, 검증 스크립트는 프로젝트 밖이라 사용자 지적대로 삭제(주소 `/uapi/domestic-stock/v1/quotations/inquire-index-timeprice`, TR `FHPUP02110200`, params U/업종코드/"60" — 검증이 필요하면 스크립트에서 직접 호출)

**9/14 섹터 카드 1개로 정정** — `report_data_service.collect_and_save_daily`는 15:30 주도 섹터 중 `max(change_rate)` 하나,
`collect_weekly_top_sector(start, end)`는 코스피 21개 업종 주별 지수 중 등락률 1위(업종마다 KIS 1회). `report_service`에서 LLM 섹터 선택(`_pick_sector_cards`, 출력 키 `sectors`) 삭제,
`REPORT_PROMPT`의 `[섹터 후보]` → `[주목할 섹터]`(섹션3은 그 업종 하나만), 응답은 `sectors` 목록 → **`sector` 단일 객체**. 테이블은 그대로(1행).
시험 생성에서 섹션3이 "하락장 속 홀로 상승"(같은 시각 다른 업종도 상승)이라 써서 비교 표현 규칙("홀로·유일하게·가장 크게"는 확정 수치로 확인될 때만)을 브리핑·보고서에 추가.
9/7주 주간 1위 = 건설 +11.03%(9/13 실측과 일치)

**9/14 `timeline_report_sector.seq` 제거** — 카드가 1개라 순서가 필요 없어 모델·리포지토리에서 뺐다.
DB 칼럼도 같은 날 15시에 삭제함(사용자가 바로 재시작하기로 해서 옛 코드 호환 단계를 생략).

**9/14 models/report.py 시안 대조 결과** — 메인·섹션1·섹션2·섹션3·결론의 네모칸을 칼럼과 한 칸씩 대조. 빠진 칸 없음.
남은 사용자 결정 2개: (1) 섹션2 차트 위 설명 문구("1,450선 돌파 후 외인 투매") 넣을지, (2) 결론 번호 항목 시안 예시가 투자 행동 권유("패닉 셀 금지")인데 코드는 권유 금지라 영역 제목과 내용이 어긋남.
기관·개인 순매수 칼럼은 화면에 없지만 섹션2 LLM 근거라 유지. 주간 상승 종목 수 구현을 위해 `kis_client.get_stock_period_price`(FHKST03010100) 추가, `get_fluctuation_ranking`에 `sort_code` 인자 추가(기존 호출은 기본값 "0"이라 동작 같음)

**9/14 일간·주간 기준 통일(사용자 결정: 주간은 일간과 똑같이, 섹터는 그 주 종합 1등)** — 차트 안 설명 문구는 더미라 구현 안 함.
- 일간 섹터 카드도 15:30 슬롯 스냅샷 대신 **20:00 보고서 시점에 21개 업종 일별 지수를 조회해 종가 기준 1위**를 고른다(주간과 같은 방식, 15:30 슬롯 실패와 무관). 9/14 장중 시험: 오락·문화 +1.20%, 13개 중 8개 상승
- 주간 투자자 순매수 합계를 일간 보고서 합 → **KIS 일별 기록의 그 주 합**으로 변경(뱃지와 같은 원천, 일간 보고서가 빠진 날이 있어도 정확). `collect_weekly_data(start, end)`에서 `daily_reports` 인자 삭제
- 9/7주 외국인 합 -2조 1,701억: 9/7 +2.55조·9/8 +0.63조 순매수가 9/10 -2.62조·9/11 -2.30조와 상쇄된 실제 값(일별 대조 확인)
- 일간·주간 항목 대응표는 `report_data_service.py` 파일 상단 주석에 정리

**9/14 결론 영역 번호 항목 정정** — 스펙 "핵심 키워드 타이틀 및 설명(3개)"를 **용어형 키워드**(연방공개시장위원회·소비자물가지수)로 잘못 해석해 생성하고 있었다.
사용자 의도는 **섹션1·2·3을 주린이 눈높이로 한 번 더 요약**(항목 i = 섹션 i). `REPORT_PROMPT`의 keywords 정의·길이(title 25자, description 1~2문장 80자)를 바꾸고
용어 나열·뜻풀이·투자 행동 지시("~하세요")를 금지. 테이블 이름은 스펙 용어(핵심 키워드)대로 `timeline_report_keyword` 유지, 주석으로 의미 명시.
시험 생성: "1. 금리 인상 경계감 / 2. 외국인 순매도 / 3. 오락·문화 업종 강세" + 쉬운 설명으로 섹션 순서와 대응 확인

**9/14 주간 비교 기준 결정** — 스펙 문구 "주간 시작일과 마지막일 비교"를 검토한 결과 **전주 대비(지금 구현) 유지**. 시작일 종가 기준은 월요일 움직임이 빠지고(9/7주 VKOSPI +17.7% → +8.5%, 건설 +11.03% → +5.92%), 거래대금 두 날 비교는 주간 값이 아니며, 외국인은 두 날 비교가 무의미해 그 주 합계가 맞다. VKOSPI·섹터 등락률·상승 비율 = 전주 마지막 거래일 종가 대비, 거래대금 = 주간 합 vs 전주 합, 외국인 = 주간 합계

**9/14 report_data_service.py 함수 점검** — 전 함수를 실호출·경계값으로 확인.
분기 창·라벨(분기·연도 경계 3/31·4/1·2027-01-04) 정상 / 투자자 기록 600거래일(2024-03-27~) / 16:07 KRX 애프터마켓 중에도 업종 output1 거래대금 = 그날 행이라 20:00 상승 종목 수 정상 /
외국인 뱃지·VKOSPI·섹터·주간 합계는 앞선 테스트대로 정상. 고친 것: 현 분기 환율(위 3번), 지난 날짜 재생성 시 21개 업종마다 찍히던 상승 종목 수 경고를 1위 카드에서 한 번만, 주간 투자자·분기 실패 로그 문구

**9/14 report_service.py · report_repository.py 점검 (20:00 첫 보고서 전)** — 코드 정독 + **9/11 일간 → 9/7주 주간 실생성**(DB·LLM·이미지·Storage 끝까지)으로 확인 후 테스트 행·이미지 삭제, 보고서 7개 테이블 id 1로 초기화.
- 일간 10초 / 주간 12초. 수치·뱃지(3거래일 연속 순매도, -2.1% 하락 / 4주 연속 순매도, +17.7% 급등)·섹터(일간 건설 +1.9%, 주간 건설 +11.03% 37개 중 20개)·분기 6개·이미지 2장·용어 3개·섹션별 쉬운 요약 3개 정상
- 조회: 일간 9/11 있음, 주간 9/11 있음, 주간 9/9 None(조회 기준대로). 재실행 중 LLM 실패 시 같은 행 유지·문구·이미지 보존 확인
- **발견·수정 1: 주간 문구가 주간 수치와 반대 방향**("외국인과 기관의 매도세" — 주간 기관은 +3조 5,900억 순매수). 일간 보고서 문장을 옮겨 씀 → `REPORT_PERIOD["WEEKLY"]`에 "숫자·방향은 확정 수치의 주간 값, 일간 보고서 문장은 옮기지 말 것" 추가, 재생성에서 "외국인 매도와 기관 매수가 엇갈린 한 주"로 정상
- **발견·수정 2: "17.67퍼센트"** — 프롬프트 규칙이 반복해서 새서 `report_service._text`에서 숫자 뒤 "퍼센트"를 "%"로 코드 치환

**9/14 섹션 정의를 설계 문구로 정정** — 프롬프트가 섹션2 "수급과 변동성", 섹션3 "업종이 어떻게·**왜** 움직였는지"로 설계와 달랐다(섹션3의 "왜"는 근거 없는 원인을 유도).
설계대로 1 핵심 이슈(원인 분석: 원인은 뉴스·타임라인에 적힌 것만) / 2 시장 전체 반응(결과 & 실증 데이터: 섹션1 이슈에 시장이 어떻게 반응했는지 확정 수치로) /
3 주목할 섹터(실제 사례: 등락률·상승 종목 비율·거래대금으로 설명, 이유는 자료에 있을 때만)로 바꾸고 모델·스키마 주석도 맞췄다.
시험 생성에서 섹션3은 데이터 중심으로 정상, 섹션2가 분기 6개 값을 전부 나열해서 "흐름과 핵심 수치 1~2개만" 규칙 추가

**9/14 prompts.py 스펙 원문 대조** — 보고서 스펙 원문·시안과 문장 단위 대조.
고친 것: (1) 섹션 설명 문단 수 — 시안은 섹션1만 두 문단, 섹션2·3은 한 문단(카드·차트가 아래 붙음)인데 전 섹션 두 문단 350자였다 → 섹션1 두 문단 350자 / 섹션2·3 한 문단 200자,
(2) 결론 영역 섹션별 쉬운 요약(keywords)에 "외국인 순매도" 같은 전문 용어가 나와서 conclusion과 같은 일상 말 규칙 추가, (3) 파일 상단 주석에 섹션별 쉬운 요약 반영.
→ 사용자 결정(시안은 더미): 섹션 설명 문단 구분 없이 **섹션1·2·3 모두 300자**. **글자 수를 모든 프롬프트에서 통일** — 제목·소제목 30자 / 한 줄 문장 80자 / 브리핑·해설 본문 150자 / 보고서 섹션 설명 300자
(브리핑 headline 20→30, subtitle 40→80, 포인트 제목 12→30, 해설 제목 25→30·본문 120→150, 보고서 title 25→30·summary 60→80·핵심 요약 60→80·키워드 제목 25→30, 용어 설명 30→80). DB 칸은 모두 이보다 크다.
**"퍼센트"→"%" 치환을 타임라인 브리핑·해설에도 적용**: `briefing_service.normalize_percent`로 옮기고 `report_service._text`도 이 함수를 쓴다

**9/14 schemas·routers 정리 + 보고서 파일 최종 점검** — 사용하지 않는 `GET /timeline/slot/{key}`·`GET /timeline/slots`와 전용 함수(`timeline_service.get_slot`·`_to_response`·`_to_sector_items`·`get_slot_list`) 삭제. 보고서 응답에서 화면에 없는 `institution_net_buy`·`individual_net_buy` 제외(DB에는 LLM 근거로 유지).
최종 점검(모델·리포지토리·수치·서비스·프롬프트·스키마·라우터 정독 + 오늘 수치 저장 없이 실호출)에서 고친 것: 모델 주석 2곳(주간 투자자 합 원천, 현 분기 환율), LLM 확정 수치에서 0 값(예: VKOSPI 보합 0.0)이 빠지던 필터, 라우터 소요 시간 주석(1분 → 10~20초).
오늘 수치 실호출(16:59): 외국인 -3조 2,943억 "4거래일 연속 순매도", VKOSPI 46.85 +1.2% 상승, 섹터 섬유·의류 +1.47% 28개 중 11개(15:30 수정 종가와 일치), 26/Q3 외국인 -27.4조(9/11까지 -24.1조 + 오늘) · 환율 1,346.0

**9/14 19:35 20:00 전 전체 점검** — 커밋 이후 바뀐 15개 파일 diff 정독(헤더 지수·타임라인·브리핑·섹터·급상승·보고서·core) + ruff F + 실행 확인(저장·LLM 없이).
20:00 슬롯 수집 0.7초(KRX TOP3 광전자·인피니트헬스케어·KS인더스트리, 뉴스 5건, 브리핑 프롬프트 형식 정상) / 헤더 지표 6종 갱신 정상(KOSPI 6,684.37 -3.26% = 종가) / API: `/timeline` 7슬롯, `/report` 없음(null), 잘못된 type 404, `/glossary` 19, 삭제한 `/slot` 404 /
일간 보고서 수치 3.1초·재료 JSON 2만 자·REPORT_PROMPT 형식 정상. **관찰: 외국인 순매수가 16:59 -3조 2,943억 → 19:36 -3조 3,331억으로 바뀜(KRX 애프터마켓 거래가 투자자 집계에 더해지는 것으로 보임)** → 20:00 이후 보고서 값이 최종에 가까움. 섹터 카드 상승 종목 수는 19:36에도 채워짐(28개 중 11개).
참고: TestClient를 lifespan 없이 요청마다 쓰면 asyncpg "attached to a different loop" 오류가 난다 — 테스트 도구 문제(요청마다 이벤트 루프가 달라짐), 서버 코드 문제 아님. 확인은 `httpx.AsyncClient(transport=ASGITransport(app))` 한 루프에서

**9/14 20:00 슬롯·첫 일간 보고서(id 1) 검증** — 로그: 20:00 슬롯 7초 저장, 보고서 11초 저장 "문구 있음, 이미지 없음" — **이미지 2장 모두 Cloudflare 429(Too Many Requests)**.
- 20:00 슬롯: KRX TOP3 광전자·빛샘전자·인피니트헬스케어 +30.00% = KRX 1분봉 `200000`과 일치, 동률은 거래량 순(2,897만 > 650만 > 56만). 빛샘전자는 15:30 11,240(+26.29%) → 애프터마켓에서 상한가 11,570. 브리핑·해설 사실은 뉴스와 일치. 해설 3이 브리핑 반복이라 정규장·애프터마켓 구분 설명으로 직접 수정
- 보고서 수치: 외국인 -3조 3,363억 "4거래일 연속 순매도" / 기관 -1조 1,869억 / 개인 +3조 351억 / VKOSPI 46.85 "+1.2% 상승" / 섹터 섬유·의류 +1.47% 28개 중 11개, 거래대금 233억(전일 104억, +125%) / 26Q3 -27.4조·1,346.1 / 용어 3개 사전 — 본문 수치와 일치
- 보고서 문장 직접 수정: (1) 섹션3 제목 "하락장 속 **홀로** 상승 1위" — 15:30 운송·창고 +1.18%·오락·문화 +1.14%도 상승 → "하락장 속 등락률 1위 기록" (비교 표현 규칙이 있는데도 또 나옴) (2) 결론 키워드 1 "중동 문제 등으로 유가가 뛰면서" — 중동·유가를 인과로 묶은 기사 없음 → 금리 우려·유가 상승 병렬로 (3) 섹션2 "외국인 순매수는 -27조…" 어색 → "27조 4,267억원을 순매도" (4) 결론 "우리 나라" → "우리나라"
- 이미지: 20:04 수동 재생성. 429는 일시적(같은 키로 곧바로 200). 장면 묘사는 LLM 응답이 저장되지 않아 요약·섹션1 설명 기준으로 규칙 12에 맞춰 직접 작성. 메인 첫 장(`main_200416.jpg`)은 사물이 아래쪽 + 하단 회색 띠라 다시 만들어 `main_200437.jpg` 저장(첫 장은 Storage에 남아 있음 — 사용자가 대시보드에서 삭제), 섹션1 `section1_200421.jpg`. 이미지 AI가 사물을 아래쪽에 두는 경향 → 사용자 이미지 프롬프트 조정 때 참고
- 코드 수정(재시작 필요, 다음 20:00 전): `image_client`가 429도 재시도(5초 x 시도 번호 대기), `report_service._write_report` 이미지 2장을 동시 요청 대신 한 장씩
- 제안(미반영): "홀로" 재발 방지로 보고서 확정 수치에 "코스피 21개 업종 중 상승 N개"를 코드가 넣기

**9/14 21시 LLM 오류 줄이기 (사용자 요청: "최대한 오류 안 나게 계속 수정")**
- **시험 후 버린 방법 — LLM 검수 단계**: 같은 모델(flash-lite)로 ① 원고 전체 검수 → 아무것도 안 고침(자동 검사가 짚은 "홀로"도 그대로) ② 근거 인용 찾기 + 코드로 인용 존재 확인 + 판정 + 문제 문장만 고치기(temperature 0) → 맞는 확정 수치 문장을 지움("4거래일 연속 순매도", "VKOSPI 1.23% 상승", "거래대금 125% 증가"), **새 오류를 만듦("3조 351억원" → "3조 351원")**, 실제 오류("중동 문제로 유가")는 못 잡음, 실행마다 결과가 다름. → 코드·프롬프트 모두 삭제
- **넣은 것 1 — 코드 자동 검사 `services/text_review.py`(LLM 호출 없음)**: 브리핑·해설(`briefing_service._check`)과 보고서(`report_service._check_content`)가 저장 전에 거친다. 퍼센트 표기 치환(`normalize_percent`가 여기로 이동), "홀로·나홀로·유일하게" 자동 삭제, 자료에 없는 숫자(두 자리 이하·연도 제외, 반올림 허용)와 17:30·20:00의 "애프터마켓에서 OO% 올랐다" 표현은 **WARNING 로그**(`자동 검사 문제`)로 남긴다 → 슬롯 검증 때 `grep "자동 검사" logs/timeline.log`. 오늘 저장 문구 전체에 돌려 오탐 0(내가 직접 계산해 넣은 20:00 해설의 26.29만 잡힘 = 정탐), 주입한 가짜 금액 "3조 4,500억원" 잡힘. 원인 날조는 코드로 못 잡는다
- **넣은 것 2 — 보고서 재료 보강**: 코스피 21개 업종 등락 분포(`report_data_service._sector_breadth`, 확정 수치 "코스피 업종 등락" — 9/14 상승 7·하락 14). `collect_and_save_daily`는 (보고서, 분포), `collect_weekly_top_sector`는 (카드, 분포)를 돌려준다(분포는 저장하지 않음). 금액을 부호 대신 방향 단어로("-3조…원" → "3조 3,363억원 순매도", 거래대금 증감 "+125%" → "125% 증가") — `_won` 삭제, `_amount`·`_flow`
- **넣은 것 3 — 보고서 문구 모델 `gemini-3.6-flash`**(`report_service._REPORT_MODEL`, 무료 하루 20회, 보고서는 하루 1~2회). 실패·필수 값 누락이면 기본 모델(flash-lite)로 한 번 더. `llm_client.generate(model=)` 인자 추가, `_MODEL` → `DEFAULT_MODEL`. 같은 9/14 재료로 비교: flash-lite는 "홀로 상승"·"중동 문제로 유가"(근거 없음), 3.6-flash는 사실 오류 없음(원인·수치·종목 모두 자료에 있음, 업종 분포를 써서 "돋보이는 흐름"). 3.6-flash가 제목에 "~했습니다"를 붙이고 "-3조…순매도"처럼 부호를 옮겨 써서 REPORT_PROMPT 규칙 8에 "제목은 명사형, 매매 금액은 방향 단어로, +/- 붙이지 말 것" 추가 → 재시험에서 둘 다 해결. 생성 20~22초(기존 약 10초). 이미지 장면 묘사는 3.6-flash가 글에 없는 사물(책상·램프)을 넣는 경향 → 이미지 프롬프트 조정 때 확인
- 브리핑·해설은 호출이 하루 16회라 flash-lite 유지(3.6-flash 한도 20회 초과)
- **재시작 필요(9/15 07:30 전)**

**9/14 보고서 형식 맞춤 + 이미지 내용 반영 (사용자 결정)**
- 사용자 결정: 오늘 데이터는 (가) 결론 키워드 제목 2개만 명사형으로 고치고("외국인의 대규모 주식 매도", "섬유·의류 업종의 상승"), **9/15부터 오늘 코드로 수집**
- 이미지가 내용과 무관하다는 피드백 → 원인: 규칙 12가 "사물 1~2개·방향 사물 금지·분위기는 빛으로만"이라 LLM이 동전·기름통 같은 흔한 사물만 골랐다
- 바꾼 방식: LLM은 영어 문장 대신 **소재 이름 2~3개 + 분위기 이름**만 고르고(`main_image`/`section1_image` = `{"symbols", "mood"}`), 코드(`report_service._image_scene`)가 `prompts.IMAGE_SYMBOLS`(보고서 주제 28개 → 구체적 사물 영어 묘사)·`IMAGE_MOODS`(상승~급락 6단계 → 날씨·빛)로 그림 설명을 만든다. 목록에 없는 이름은 버리고 경고, 소재가 없으면 "코스피·증시 전체". `IMAGE_STYLE`은 "하나의 장면으로 가운데 모음 + no faces"로 수정(빈 배경 위아래 문구 제거)
- 시험: 로봇 머리 소재는 해골처럼 그려져 AI 칩으로 교체, 중앙은행 박공에 가짜 글자("If")가 나와 "plain blank pediment" 추가, 외국인 자금(보물상자)이 안 읽혀 "지구본 + 거의 빈 동전 유리병"으로 교체. 번개는 과해서 급락도 "dark gray storm clouds"
- 오늘 보고서 이미지 교체(20:40): 메인 = 중앙은행·유가·외국인 자금 + 급락(`main_204049.jpg`), 섹션1 = 3.6-flash가 고른 중앙은행·AI·유가 + 급락(`section1_204049.jpg`). **Storage에 안 쓰는 이미지 3장**: `daily/2026-09-14/main_200416.jpg`, `main_200437.jpg`, `section1_200421.jpg` — 사용자가 대시보드에서 삭제

**9/14 이미지 방향 비교 → "이야기 장면" 채택 (사용자: "1·2·3 전부 확인해보고 가장 좋은 방향으로")**
- 소재 나열 방식도 "이미지만 보고 내용을 짐작할 수 없다"는 피드백. 오늘 메인 요약으로 방향마다 2장씩 비교(scratchpad `cmp/sheet.jpg`, 사용자에게 전달):
  (1) 소재 나열 — 정적, 가짜 글자 "BANK" / **(2) 이야기 장면(서울 도심 주인공 + 멀리 원인 + 도시에서 날아가는 동전)** — 가장 잘 읽힘, 1장에 가짜 글자 /
  (3a) 계단 위 동전 흘러내림 — 하락 방향은 2장 모두 맞음, 서울이 묻히고 복잡, 작은 미국 국기 / (3b) 가라앉는 풍선 — 오르는지 내리는지 모호
- 구현: `prompts.IMAGE_SCENE`(+`IMAGE_SCENE_NO_CAUSE`) 틀 = `IMAGE_CITY`(서울 도심·타워) + `IMAGE_MOODS` + 지평선의 원인 소재 1~2개 + `IMAGE_FLOWS`(외국인 매도 → 도시에서 동전이 날아감 / 매수 → 동전이 도시로 / 없음). LLM 출력 `{"symbols", "flow", "mood"}`, `report_service._image_scene`이 채움. "코스피·증시 전체" 소재 삭제(도시가 고정), "외국인 자금" → flow로 대체하고 "해외 증시"(지구본) 추가
- 추가 시험: 원인 소재를 **실루엣**으로 쓰면 박공 가짜 글자가 사라짐(4장 중 0장), `image_client._STEPS` **4 → 8**이면 사물이 뚜렷해짐 → 둘 다 적용. 칩(AI·반도체)은 건물 사이에 묻혀 안 보여 **하늘에 띄움**("floating high in the sky") → 2장 모두 보임
- LLM이 요약에 없는 "환율·달러"를 외국인 자금 대용으로 고름 → 규칙 12에 "글에 직접 나온 대상만, 외국인 매매는 flow로" 추가 → 재시험에서 금리·AI + 외국인 매도로 정확히 고름
- 오늘 보고서 이미지 최종 교체(20:56): `main_205612.jpg`(금리·유가 + 외국인 매도 + 급락), `section1_205612.jpg`(AI·금리 + 외국인 매도 + 급락, 5장 중 가장 잘 읽히는 것). 이미지 AI는 매번 결과가 달라 가끔 가짜 글자·소재 누락이 남는다
- **Storage에 안 쓰는 오늘 이미지**(사용자가 대시보드에서 삭제): `daily/2026-09-14/` 아래 `main_200416.jpg`, `main_200437.jpg`, `section1_200421.jpg`, `main_204049.jpg`, `section1_204049.jpg`

**9/14 21시 이미지 모델 비교 (사용자: 증권 리포트 느낌, 차트·화살표 제한 없이, 마음에 안 들면 플랫폼 변경 검토)**
- 계정에서 쓸 수 있는 Workers AI 이미지 모델(모델 검색 API로 조회): flux-1-schnell(현재), flux-2-dev, flux-2-klein-4b/9b, leonardo/lucid-origin, leonardo/phoenix-1.0, SDXL 계열
- 요청 형식: flux-2는 multipart 폼(prompt·width·height) → JSON `result.image`(base64) / lucid-origin은 JSON → `result.image` / phoenix는 JSON → **JPEG 바이너리 그대로**. flux-2·Leonardo는 가로형 크기 지정 가능(1024×576 시험)
- 같은 프롬프트(오늘 메인 요약, 리포트 표지, 파란 하락 차트·화살표, 서울·연준·원유, 라벨 "KOSPI -3.26%") 1장씩 (scratchpad `models/sheet.jpg`, 사용자에게 전달):
  **lucid-origin** 3초, 하락 방향 맞음, 리포트 느낌 가장 좋음, 라벨 숫자 틀림("-8.26%") / phoenix 4초, 라벨 맞음, 차트는 상승(틀림), 깨진 한글 다수 / flux-2-klein-9b 3.5초, 상승(틀림), 깨진 한글 다수 / flux-2-dev **229초**, 가장 정교, 상승(틀림), 영어 문장 깨짐 / schnell 2초, 하락 방향, 조잡
- 판단: lucid-origin 우선 후보. 라벨을 요구하면 다른 글자도 같이 생성하므로 **이미지에는 글자를 넣지 않고 숫자·제목은 프런트 텍스트로**
- **21:10 무료 한도(하루 10,000 뉴런) 소진** — 오늘 schnell 약 30장 + 상위 모델 4장. 한도는 09:00(KST, UTC 자정)에 초기화. lucid-origin 글자 없는 버전 방향 일관성 시험 2장은 못 함
- 비용 주의: Leonardo 단가 표기(512×512 타일당 $0.007 + 단계당) 기준 1024×576 장당 수천 뉴런으로 추정 → 일간 2장은 무료 안, 금요일(일간+주간 4장)은 한도에 걸릴 수 있음. **실제 소모는 대시보드에서 확인 필요**
- 코드는 아직 schnell + 이야기 장면 방식 그대로(9/15 20:00도 이대로 돈다). 모델 교체는 9/15 09:00 이후 시험 결과를 보고 사용자와 결정

**9/15(화) 할 일 (사용자와 확정)**
1. 07:30 전 서버 재시작 (9/14 밤 수정 반영: 이미지 429 재시도·순차 생성, text_review 자동 검사, 보고서 3.6-flash, 업종 분포·금액 방향 단어, 이미지 이야기 장면·실루엣·8단계)
2. 09:00(Cloudflare 한도 초기화) 이후 **9/14 보고서 이미지 2장을 lucid-origin으로 다시 만들어 DB 저장**(문구는 그대로). 짧은 영단어만, 숫자 없이. 차트 방향·철자·가짜 글자 확인
3. Cloudflare 대시보드에서 lucid-origin 장당 뉴런 소모 확인 → 20:00 보고서 몫(2장) 남는지 계산. 팀 상의 후 모델 교체 결정(교체 시 20:00 전 코드 수정·재시작)
4. 슬롯마다 저장값·문장 대조: 07:30 등락률 0.00% 아닌지 / 08:30 NXT TOP3 / 09:30~14:00 섹터 / **15:30(15:35 수집) 종가·업종 순위** / **17:30·20:00 KRX TOP3·전일 종가 대비 표현** / 20:00 보고서(3.6-flash 첫 실사용). 매번 `grep -E "WARNING|ERROR|자동 검사" logs/timeline.log`
5. 나노바나나(Gemini 이미지): 무료 한도 0 확인(429 `limit: 0`, 2.5-flash-image·3.1-flash-lite-image). Gemini 앱 구독과 API 결제는 별개. 쓰려면 이미지 전용 프로젝트·키에 결제 연결 — 팀 결정 대기

**9/14 확인할 것 (보고서)**
1. 20:00 슬롯 뒤 로그에 일간 보고서 저장 완료가 찍히는지, 섹터 카드 상승 종목 수가 채워지는지(당일이면 채워져야 함)
2. 문구 품질 — 섹션2가 수치 나열에 그침(9/10 실행), 섹션3이 등락률 반복. 프롬프트에서 "왜"를 더 요구할지 검토
3. ~~분기 차트 현 분기 환율~~ → **9/14 원인 확인·수정**: 월별(M) 조회는 종료일이 최근 거래일보다 앞이면 이번 달 행을 안 준다
   (종료일 9/10 → 9월 행 없음, 9/11 → 있음). 현 분기는 **일별(D)에서 보고서 날짜 이하 가장 최근 값**을 쓰도록 바꿨다. 9/10 재계산 26/Q3 = 1,349.9(그날 값)
4. 주간 보고서는 9/18(금) 20:00 이후 처음 자동 생성된다(추석 연휴 주는 마지막 거래일 기준)
5. 이미지가 디자인 톤에 맞는지 — 안 맞으면 `prompts.IMAGE_STYLE`과 프롬프트 13번 규칙 조정
7. ~~주간 섹터 카드 상승 종목 수가 큰 업종에서 틀린다~~ → **9/14 (가)로 구현 완료(사용자 결정)**: `kis_client.get_kospi_master()`(zip 받아 압축 해제, `_send_with_retry`로 재시도 공유) + `report_data_service._sector_members`(바이트 위치로 업종 코드·거래정지 해석). 명단이 업종 지수 종목 수와 다르면 경고, 못 찾으면 상승 종목 수만 비움. 순위 API 명단 코드와 `get_fluctuation_ranking`의 `sort_code` 인자 삭제. 실제 함수로 9/7주: 건설 35개 중 20개(주간 1위 카드 전체 4초), 화학 122개 중 43개(9초), 명단 수 건설 35·화학 122·금융 141·부동산 22 = 지수와 일치.
   (아래는 발견 당시 기록)  — 종목 목록을 등락률 순위(상승 30 + 하락 30)로 모으는데 순위 API는 정렬마다 30행이 상한이다.
   코스피 업종 종목 수(9/14 output1): 화학 122 / 금융 141 / 전기·전자 79 / 운송장비·부품 62 / 금속 61 / 유통 61 → 이 6개가 주간 1위면 "60개 중 N개"처럼 전체·상승 수가 모자란다.
   정렬 0~4를 다 합쳐도 화학 78/122, 금융 87/141. 작은 업종도 순위 목록이 지수 종목 수와 다르다(건설 순위 37 / 지수 35 — 일간 카드는 35 기준).
   선택지: (가) KIS 종목 마스터 파일(kospi_code.mst, 업종 분류 포함)로 업종 전 종목 확보 (나) 순위 목록 종목 수가 지수 종목 수와 다르면 상승 종목 수를 비움 (다) 지금대로 두고 경고만. 첫 주간 보고서 9/18 전 결정
   **(가) 검증 결과 (9/14 17시, scratchpad 테스트만 — 코드 미반영)**: `https://new.real.download.dws.co.kr/common/master/kospi_code.mst.zip`(120KB, 매일 갱신, 로그인 불필요).
   줄마다 **바이트 기준** 고정 폭(종목명 21~61바이트, cp949 — 문자 기준으로 자르면 어긋난다). 그 뒤 그룹코드 2 / 시총규모 1 / 지수업종 대·중·소분류 각 4자리 = **지수 업종코드와 같은 체계**
   (건설 대분류 `0018`, 화학·전기·전자는 대분류 `0027`(제조) + 중분류 `0008`·`0013`). 필드 폭은 KIS 공식 예제 순서, 상장일자(현대건설 19841222)로 위치 검증.
   **ETF·ETN 등(그룹 EF·EN·BC·SW) 제외 + 거래정지(Y) 제외**하면 21개 업종 종목 수가 업종 지수 output1 합계와 **전부 일치**(화학 122, 금융 141, 건설 35, 부동산 22 = 리츠 RT 포함, 우선주 포함).
   이 명단으로 9/7주 주봉 비교: 건설 35개 중 20개(2.2초, 순위 방식은 37개 중 20개), 화학 122개 중 43개(7.6초), 주봉 없는 종목 0
6. **문장 끝맺음(보류, 사용자 결정 대기)** — 지금은 브리핑(규칙 10)·해설(규칙 9)·보고서(규칙 8) 모두 "~습니다"로 통일.
   사용자 제안: 핵심 요약·브리핑은 "~원리"처럼 명사형 허용, 섹션 설명·결론 섹션별 쉬운 요약 설명은 존댓말.
   **9/14 20:00 첫 일간 보고서 결과를 보고 결정**(정해지면 항목별로 규칙을 나눈다)

**2단계 결과 (2026-09-13, 전부 새 함수로 실호출 확인)**

| 함수 | 용도 | 확인 값 |
|---|---|---|
| `kis_client.get_investor_daily_by_market(base_date)` | 코스피 투자자별 순매수 (output 300거래일, 최신부터) | 9/11 외국인 `frgn_ntby_tr_pbmn` -2,298,412(백만원) |
| `kis_client.get_index_daily_price(index_code, base_date, period)` | 업종 지수 D/W. output1 = 최근 거래일 현재 값(종목 수·전일 거래대금), output2 = 기간별 | VKOSPI `0503` W 9/7주 46.28, +17.67% / 건설 `0018` D `acml_tr_pbmn` 607,497 = 6,075억 → **거래대금도 백만원** |
| `kis_client.get_overseas_period_price(market, symbol, start, end, period)` | 환율 기간별 (output2 최신부터, 월별 날짜는 그달 1일) | `FX@KRW` M 2025-04~2026-09 18행, 9월 1,344.1 |
| `image_client.generate_image(prompt)` | 영어 프롬프트 → JPEG 바이트 | 168KB, 1.8초 |
| `storage_client.upload_file(bucket, path, content, content_type)` | 업로드 후 공개 URL 반환 (`x-upsert` 덮어쓰기) | 버킷 `report-images` 업로드 → 공개 GET 200 → 테스트 파일 삭제 |

- **공개 URL은 CDN 캐시가 있다.** 파일을 지운 뒤에도 같은 URL이 한동안 200으로 옛 이미지를 줬다(쿼리 붙이면 400).
  → 이미지 경로에 **생성 시각을 넣어** 재생성하면 새 URL이 되게 했다 (예: `daily/2026-09-14/main_200512.jpg`)
- 섹터 종목 수 필드: `ascn_issu_cnt`(상승) / `down_issu_cnt`(하락) / `stnr_issu_cnt`(보합). 전체 = 세 값의 합 (건설 26+8+1=35)

**3단계 결과 (2026-09-13, 2000년 날짜 테스트 행으로 DB 실측 후 삭제)**

`services/report_repository.py` — 서비스는 **세 번에 나눠 저장**한다. 앞 단계가 성공하면 뒤 단계가 실패해도 앞 값은 남는다.

| 함수 | 하는 일 | 재실행 동작 (확인함) |
|---|---|---|
| `save_report_data(session, report_type, start_date, end_date, data)` | 수치 저장. 없으면 행 생성 | 넘긴 키만 바꾸고 문구·이미지는 유지. `quarters`/`sectors` 키가 있으면 자식 행 전체 교체(옛 행 삭제됨) |
| `save_report_content(session, report_type, start_date, content)` | LLM 문구 저장 + `published_at` 기록. **LLM 성공 시에만 호출** | 섹션·요약·키워드·용어 교체, **이미지 URL은 비움**(문구와 안 맞으므로) |
| `save_report_images(session, report_type, start_date, main_image_url=, section1_image_url=)` | 이미지 URL 저장 | None으로 넘긴 쪽은 안 건드림 |
| `load_report(session, report_type, target_date)` | 프런트 조회. 일간 = 시작일 일치, 주간 = 종료일 일치(그날 만든 것만) | 주 중간·주말 날짜는 주간 None |
| `load_daily_reports(session, start_date, end_date)` | 주간 재료, 날짜순 | |

- 상수 `report_repository.DAILY` / `WEEKLY`를 쓴다. 보고서가 없는데 content/images를 부르면 `ValueError`
- `data`·`content` dict 키 형식은 함수 위 주석에 적었다. 서비스(`report_data_service`·`report_service`)가 이 형식으로 넘긴다
- 주간 섹터 카드는 LLM이 고른 뒤 `save_report_data(..., {"sectors": [...]})`만 다시 부르면 된다
- 이미지 저장 경로에는 생성 시각을 넣는다(2단계 결과의 CDN 캐시 참고)

**4-1 결과 (2026-09-13 구현)**

`services/report_data_service.py`
- `collect_daily_data(day, sector_names)` (동기) → `save_report_data`의 `data` 형식 dict. 항목별 try/except, 실패 항목은 키를 빼서 기존 값 유지
- `collect_and_save_daily(session, day=None)` (async) → 그날 `15:30` 슬롯의 주도 섹터를 등락률 순으로 읽어 수집 후 저장. `report_service.generate_daily`가 부른다
- 섹터 카드: 등락률·거래대금은 output2 그날 행, 전일 거래대금은 그다음 행(날짜 지정이라 나중에도 정확).
  상승·전체 종목 수는 output1 거래대금이 그날 행과 같을 때만 채운다(최근 거래일 검증). 업종명이 표에 없으면 이름만 있는 카드 + 경고
- 분기 차트: 투자자 API를 기준일을 옮겨 최대 3회 호출해 6분기 시작일까지 모은다. 환율은 분기 안 가장 늦은 달 월간 값, 0 값 제외
- 스모크 테스트(9/11, DB 저장 없이): 외국인 -2,298,412 / 기관 -1,218,394 / 개인 1,867,544, VKOSPI 46.28 -2.12%,
  분기 25/Q2 -5.5조·1,355.3 … 26/Q2 -91.7조·1,549.5, 26/Q3(현재) -24.1조·1,344.1, 건설 +1.90% 26/35 607,497(전일 466,923), 없는 업종명 → 경고 + 빈 카드
- 휴장일 판단은 여기서 하지 않는다(`report_service.run_scheduled_reports`가 먼저 확인한다)

1. **보고서 확인·조정** — 코드는 1~6단계 모두 구현됨. 사용자 확인(9/14) 후 위 '9/14 확인할 것' 반영. 스펙은 '보고서(브리핑 탭) 설계 — 확정'.
   - 새 테이블은 FK `ondelete="CASCADE"` + ORM `cascade` 둘 다, `created_at`은 파이썬 기본값 + DB `DEFAULT` 둘 다, `_EAGER_LOAD` 등록
   - 9/10~9/11 테스트 데이터는 9/13 밤 삭제 완료(백업 위치는 '실데이터 적재 일정')
   - 9/13 수정·추가한 파일(타임라인 주석 정리 + 보고서)은 9/13 밤 커밋 완료. 확인 후 고칠 것은 새 커밋으로
2. **LLM 품질 검토** — 용어 사전(`glossary.GLOSSARY`), 슬롯별 검색어(`news_service._SLOT_KEYWORDS`),
   집중 지점(`prompts.SLOT_FOCUS`)을 주식 잘 아는 사람에게 검토받고 팀 회의에서 조정
3. **프런트 반영 방식** — 폴링으로 시작. 프런트 담당자와 주기 협의.
   용어 호버 설명은 `GET /timeline/glossary`를 쓰면 된다(프런트에 전달할 것)
4. **배포 준비** — 실제 배포 시점에 다시 논의하기로 했다. 지금 구조로 올리면 걸리는 것 세 가지.
   - **KIS 토큰 캐시가 로컬 파일이다**(`.cache/kis_token.json`, 유효 24시간). 컨테이너
     파일시스템은 재배포마다 초기화되므로 그때마다 토큰이 재발급되고 **계좌 주인에게 알림이 간다.**
     DB 테이블로 옮기면 해결된다(Postgres가 이미 있다)
   - **무료 티어가 앱을 잠재우면 스케줄러가 죽는다.** 하루 8번 예약 수집이 서비스의 핵심인데
     무접속 시 spin down되면 슬롯이 안 쌓인다. 항상 켜진 티어를 쓰거나, 외부 cron이
     `POST /timeline/collect/{slot}`을 때리는 방식으로 바꿔야 한다(그러면 APScheduler를 빼도 된다).
     단 그 엔드포인트는 지금 인증이 없어서 아무나 수집을 트리거할 수 있다 — 막아야 한다.
     `POST /timeline/report/daily`·`/weekly`도 같다(한 번 부를 때마다 제미나이 1회 + 이미지 2장)
   - **인스턴스가 2개 이상이면 수집이 중복된다.** 데이터는 안 깨지지만 제미나이 호출이 2배가 된다
